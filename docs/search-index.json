[{"page":"The CSP-safe evaluator","category":"Advanced","heading":"The CSP-safe evaluator","slug":"advanced-evaluator","anchor":"","snippet":"Every Summit directive contains a JavaScript expression: s-text=\"count\", @click=\"open = !open\", :class=\"{ active: selected }\". Most HTML-first frameworks turn those strings into functions with new Function or eval. That ","text":"the csp-safe evaluator every summit directive contains a javascript expression: s-text=\"count\", @click=\"open = !open\", :class=\"{ active: selected }\". most html-first frameworks turn those strings into functions with new function or eval. that requires unsafe-eval in your content-security-policy, which is exactly the thing a strict policy is meant to forbid. summit takes a different path. it ships its own small javascript engine and never calls eval or new function, so it runs under a strict csp with the standard build and nothing to configure."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"Why no eval","slug":"advanced-evaluator","anchor":"why-no-eval","snippet":"A Content-Security-Policy that omits unsafe-eval blocks eval, new Function, and the string forms of setTimeout/setInterval at runtime, in the browser. Any framework that compiles expressions with those APIs simply stops ","text":"why no eval a content-security-policy that omits unsafe-eval blocks eval, new function, and the string forms of settimeout/setinterval at runtime, in the browser. any framework that compiles expressions with those apis simply stops working under such a policy unless you either loosen the policy or ship a separate, restricted build. because summit interprets expressions itself, there is nothing for the policy to block. the same script tag works whether or not your site sets a strict csp, and you never maintain two builds."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"The pipeline","slug":"advanced-evaluator","anchor":"the-pipeline","snippet":"An expression string travels through three stages before it produces a value. 1. Lexer. tokenize scans the string into a flat list of tokens: numbers, strings, template literals, identifiers, and a fixed set of operators","text":"the pipeline an expression string travels through three stages before it produces a value. 1. lexer. tokenize scans the string into a flat list of tokens: numbers, strings, template literals, identifiers, and a fixed set of operators and punctuators. 2. parser. parse reads those tokens into an abstract syntax tree, a structured description of the expression. the set of node shapes the parser can produce is the whole language: anything it cannot build is a parse error. 3. interpreter. interpreter.run walks the tree and computes a value, resolving names against your component's scope. it is a plain tree walk, no code generation of any kind. parsed trees are cached by their source and mode, so an expression that a directive re-evaluates on every state change is parsed once and interpreted many times. there are two parse modes, and the caller chooses based on what it wants: - expression mode parses a single value expression, used by value directives like s-data, s-text, :bind, and s-show. here a leading { is an object literal. - program mode parses a list of statements, used by action directives like s-on handlers and s-init. here a leading { is a block. that split is how summit resolves the classic ambiguity of { } without guessing. the directive already knows whether it wants a value or an action."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"What syntax is supported","slug":"advanced-evaluator","anchor":"what-syntax-is-supported","snippet":"The parser and interpreter implement a broad, practical subset of JavaScript expression syntax. All of the following work inside directives: - Literals: numbers (including floats, exponents, and hex like 0xff), single- a","text":"what syntax is supported the parser and interpreter implement a broad, practical subset of javascript expression syntax. all of the following work inside directives: - literals: numbers (including floats, exponents, and hex like 0xff), single- and double-quoted strings with escapes, template literals with ${...} interpolation, true, false, null, undefined, and this. - objects and arrays, including shorthand properties, computed keys, method shorthand, getters and setters, array holes, and spread ({ ...a }, [ ...xs ]). - member access with . and [ ], and optional chaining ?. for property reads, computed reads, and calls. - function calls with spread arguments and optional calls (fn?.()). - operators: ! - +, typeof, void, prefix and postfix ++/--, arithmetic + - / % , comparison < <= =, equality == != === !==, in, instanceof, logical && || ??, the ternary ?:, and assignment in all its forms (= += -= = /= %= = &&= ||= ??=). - arrow functions, with expression or block bodies, default parameters, destructuring parameters, and rest parameters. in program mode (action directives) you additionally get statements: if/else, return, let/const/var with destructuring, for, for...of, while, break, continue, and blocks. this is why @click handlers can hold real multi-statement logic."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"What is not supported","slug":"advanced-evaluator","anchor":"what-is-not-supported","snippet":"The subset stops where an expression language should. If you write any of these in a directive, you get a parse error, which Summit catches and logs to the console rather than executing: - The new operator. Constructors ","text":"what is not supported the subset stops where an expression language should. if you write any of these in a directive, you get a parse error, which summit catches and logs to the console rather than executing: - the new operator. constructors are not callable from expressions, so use a static method or a global that returns a value. reach for date.now() rather than new date(), and build the object in a summit.data provider if you need a constructed instance. - bitwise operators (& | ^ ~ << ). - the function keyword and function declarations. use arrow functions. - classes, try/catch, throw, switch, do...while, async/await, and generators. - comments, regex literals (use the regexp global), and tagged template literals. keeping the surface bounded is not only about safety. it also keeps directive expressions readable. anything more involved belongs in a summit.data method, where you write ordinary javascript that your bundler and csp already trust."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"How names resolve","slug":"advanced-evaluator","anchor":"how-names-resolve","snippet":"When the interpreter meets an identifier, it looks in three places, in order: 1. Interpreter-local bindings: arrow-function parameters and any let/const declared in the same expression. 2. The component scope: the merged","text":"how names resolve when the interpreter meets an identifier, it looks in three places, in order: 1. interpreter-local bindings: arrow-function parameters and any let/const declared in the same expression. 2. the component scope: the merged s-data stack, innermost first, so an inner component shadows an outer one, plus the $-prefixed magics. 3. the global allowlist: a fixed set of safe globals (below). names in the allowlist resolve to the real global; anything else resolves to undefined. reads are deliberately tolerant. accessing a property of null or undefined yields undefined instead of throwing, and calling a method that does not exist is a no-op that returns undefined. this keeps templates resilient to half-loaded data. assigning to a property of null or calling a value that is present but not a function still throws, because those are real mistakes. arrow functions capture this lexically from the surrounding component, while methods and getters defined in an object literal receive the usual dynamic this, matching how the same code behaves in a normal engine."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"The global allowlist","slug":"advanced-evaluator","anchor":"the-global-allowlist","snippet":"An expression can only name a global if it is on the allowlist. The default set covers the common, safe surface: To let expressions reach a global you provide, add it: Now dayjs() resolves inside any directive. The allow","text":"the global allowlist an expression can only name a global if it is on the allowlist. the default set covers the common, safe surface: to let expressions reach a global you provide, add it: now dayjs() resolves inside any directive. the allowlist is a scoping and ergonomics boundary: it controls which names an expression may mention, and it keeps a typo from silently reaching an unrelated global. it is not a full sandbox, because allowlisted globals expose their whole surface. the point that matters for csp is narrower and firmer: summit itself never needs unsafe-eval."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"Under a strict CSP","slug":"advanced-evaluator","anchor":"under-a-strict-csp","snippet":"Because the evaluator uses no dynamic code, Summit is happy with a policy that forbids it entirely: Load Summit as a normal script under that header and every directive works. There is no unsafe-eval, no unsafe-inline re","text":"under a strict csp because the evaluator uses no dynamic code, summit is happy with a policy that forbids it entirely: load summit as a normal script under that header and every directive works. there is no unsafe-eval, no unsafe-inline required for the framework, and no alternate build to swap in. the browser is still the ultimate enforcer: even the allowlisted window cannot be used to smuggle in window.eval(...), because the browser blocks eval at runtime regardless of who calls it. summit's contribution is that it never asks you to open that door in the first place."},{"page":"The CSP-safe evaluator","category":"Advanced","heading":"See also","slug":"advanced-evaluator","anchor":"see-also","snippet":"- Reactivity for how the values these expressions read are tracked. - Extending Summit to reach the evaluator from your own directives with utils.evaluate and utils.evaluateAction.","text":"see also - reactivity for how the values these expressions read are tracked. - extending summit to reach the evaluator from your own directives with utils.evaluate and utils.evaluateaction."},{"page":"Writing directives and plugins","category":"Advanced","heading":"Writing directives and plugins","slug":"advanced-extending","anchor":"","snippet":"Summit's built-in directives and magics are not privileged. They are registered through the same public API you use for your own, and they receive the same utilities. So a directive you write is a first-class citizen: it","text":"writing directives and plugins summit's built-in directives and magics are not privileged. they are registered through the same public api you use for your own, and they receive the same utilities. so a directive you write is a first-class citizen: it can evaluate expressions in the element's scope, create reactive effects, and clean up after itself, exactly like s-text does. this page shows how to build a custom directive, a custom magic, and how to bundle both into a reusable plugin."},{"page":"Writing directives and plugins","category":"Advanced","heading":"A custom directive","slug":"advanced-extending","anchor":"a-custom-directive","snippet":"Register a directive with Summit.directive. You pass a name without the s- prefix and a handler function. The name becomes the attribute: registering \"money\" gives you s-money. That is the whole thing, and it is reactive","text":"a custom directive register a directive with summit.directive. you pass a name without the s- prefix and a handler function. the name becomes the attribute: registering \"money\" gives you s-money. that is the whole thing, and it is reactive. utils.effect runs the callback once and re-runs it whenever any value the callback read changes. because the callback reads price through utils.evaluate, the text updates the moment price changes, and only that text node updates."},{"page":"Writing directives and plugins","category":"Advanced","heading":"The handler arguments","slug":"advanced-extending","anchor":"the-handler-arguments","snippet":"Your handler is called with three arguments. el is the element the directive is on. meta describes the parsed attribute: - meta.name is the directive name (\"money\"). - meta.value is the part after a colon, or null. For s","text":"the handler arguments your handler is called with three arguments. el is the element the directive is on. meta describes the parsed attribute: - meta.name is the directive name (\"money\"). - meta.value is the part after a colon, or null. for s-interval:500 it is \"500\". - meta.modifiers is the dot-separated list. for @click.prevent.once it is [\"prevent\", \"once\"]. - meta.expression is the attribute's value, the expression string. - meta.raw is the original attribute name as written. utils is the directive author's toolkit: - utils.evaluate(expression) evaluates a value expression in this element's scope and returns the result. - utils.evaluateaction(expression, locals?) evaluates one or more statements, optionally with extra local names (this is how s-on injects $event). - utils.effect(fn) creates a batched reactive effect that is torn down automatically when the element is removed. - utils.cleanup(fn) registers a teardown callback. - utils.scopes is the scope stack visible at this element, and utils.inittree / utils.destroytree let a directive set up or tear down a subtree it manages. - utils.summit is the summit global, and utils.makeenv builds a fresh evaluation environment for another element."},{"page":"Writing directives and plugins","category":"Advanced","heading":"Reading modifiers and cleaning up","slug":"advanced-extending","anchor":"reading-modifiers-and-cleaning-up","snippet":"A directive that is not purely reactive uses meta.value, evaluateAction, and cleanup. Here is one that runs an action on an interval and tears the timer down with the element: Anything you register with utils.cleanup run","text":"reading modifiers and cleaning up a directive that is not purely reactive uses meta.value, evaluateaction, and cleanup. here is one that runs an action on an interval and tears the timer down with the element: anything you register with utils.cleanup runs when the element leaves the dom. effects created with utils.effect are cleaned up for you, so you only need cleanup for listeners, timers, and observers you create yourself."},{"page":"Writing directives and plugins","category":"Advanced","heading":"Priority","slug":"advanced-extending","anchor":"priority","snippet":"A third argument sets the directive's priority. Lower numbers run earlier on the same element, and the default is 100. For reference, the built-ins run in this order: s-ref (10), s-id (15), the structural s-if/s-for/s-te","text":"priority a third argument sets the directive's priority. lower numbers run earlier on the same element, and the default is 100. for reference, the built-ins run in this order: s-ref (10), s-id (15), the structural s-if/s-for/s-teleport (20), s-bind (50), s-model (60), s-on (70), s-text/s-html (80), s-transition (85), s-show (90), s-effect (100), s-init (200), and s-cloak (1000). the structural directives are a fixed internal group that take over their own subtree; a custom directive runs in priority order after the scope and any structural directive are set up, and summit still descends into the element's children afterward."},{"page":"Writing directives and plugins","category":"Advanced","heading":"A custom magic","slug":"advanced-extending","anchor":"a-custom-magic","snippet":"A magic is a $-prefixed helper available in every expression. Register one with Summit.magic. The factory receives a context object and returns either a value or a function. The factory's context gives you the element an","text":"a custom magic a magic is a $-prefixed helper available in every expression. register one with summit.magic. the factory receives a context object and returns either a value or a function. the factory's context gives you the element and scope plus the same evaluation and lifecycle helpers directives get: return a plain value for a property-style magic, or a function for a callable one. magics are also exposed on the component scope, so inside a summit.data method you can reach them as this.$copy, this.$refs, and so on."},{"page":"Writing directives and plugins","category":"Advanced","heading":"Bundling a plugin","slug":"advanced-extending","anchor":"bundling-a-plugin","snippet":"A plugin is just a function that receives the Summit global. Use Summit.plugin to run it. Everything a plugin registers, directives, magics, data providers, stores, and extra globals, lives together in one installable un","text":"bundling a plugin a plugin is just a function that receives the summit global. use summit.plugin to run it. everything a plugin registers, directives, magics, data providers, stores, and extra globals, lives together in one installable unit. summit.plugin calls your function immediately with the global and returns the global, so registration methods chain:"},{"page":"Writing directives and plugins","category":"Advanced","heading":"When to register","slug":"advanced-extending","anchor":"when-to-register","snippet":"Register your directives, magics, and plugins before Summit starts, so they are in place when the page is first initialized. With the CDN build, which starts on its own once the DOM is ready, put your registrations in a ","text":"when to register register your directives, magics, and plugins before summit starts, so they are in place when the page is first initialized. with the cdn build, which starts on its own once the dom is ready, put your registrations in a script that runs before that, or register them inside a summit:init listener: registration itself is timing-safe: the order of your registrations does not matter, and registering a name that already exists overrides the previous handler, which is how you can replace a built-in directive with your own."},{"page":"Writing directives and plugins","category":"Advanced","heading":"See also","slug":"advanced-extending","anchor":"see-also","snippet":"- Summit.directive, Summit.magic, and Summit.plugin for the reference details. - How reactivity works for what utils.effect does under the hood. - The CSP-safe evaluator for what utils.evaluate accepts.","text":"see also - summit.directive, summit.magic, and summit.plugin for the reference details. - how reactivity works for what utils.effect does under the hood. - the csp-safe evaluator for what utils.evaluate accepts."},{"page":"Migrating from Alpine","category":"Advanced","heading":"Migrating from Alpine","slug":"advanced-migrating","anchor":"","snippet":"If you know Alpine, you already know Summit. The mental model is the same: state declared on an element, expressions in attributes, @ for events and : for bindings. Most of a migration is a find-and-replace of the direct","text":"migrating from alpine if you know alpine, you already know summit. the mental model is the same: state declared on an element, expressions in attributes, @ for events and : for bindings. most of a migration is a find-and-replace of the directive prefix. this page covers that rename and then the handful of places where summit behaves differently on purpose."},{"page":"Migrating from Alpine","category":"Advanced","heading":"The prefix rename","slug":"advanced-migrating","anchor":"the-prefix-rename","snippet":"Alpine's directives start with x-. Summit's start with s-. The two shorthands are identical: @click is still @click, and :class is still :class, because those are just short forms of s-on: and s-bind:.","text":"the prefix rename alpine's directives start with x-. summit's start with s-. the two shorthands are identical: @click is still @click, and :class is still :class, because those are just short forms of s-on: and s-bind:."},{"page":"Migrating from Alpine","category":"Advanced","heading":"Directive mapping","slug":"advanced-migrating","anchor":"directive-mapping","snippet":"Every Alpine directive has a same-named Summit counterpart. Rename the prefix and you are done. The magics keep their names too: $el, $refs, $store, $watch, $dispatch, $nextTick, $root, and $id all work as you expect. Su","text":"directive mapping every alpine directive has a same-named summit counterpart. rename the prefix and you are done. the magics keep their names too: $el, $refs, $store, $watch, $dispatch, $nexttick, $root, and $id all work as you expect. summit adds one, $data, which returns the nearest scope object. the global api maps one for one as well: alpine.data, alpine.store, alpine.bind, alpine.directive, alpine.magic, alpine.plugin, and alpine.start become summit.data, summit.store, summit.bind, summit.directive, summit.magic, summit.plugin, and summit.start."},{"page":"Migrating from Alpine","category":"Advanced","heading":"What is genuinely different","slug":"advanced-migrating","anchor":"what-is-genuinely-different","snippet":"The rename gets you a working page. These differences are where Summit is worth understanding, because they change what you can rely on.","text":"what is genuinely different the rename gets you a working page. these differences are where summit is worth understanding, because they change what you can rely on."},{"page":"Migrating from Alpine","category":"Advanced","heading":"Fine-grained signals","slug":"advanced-migrating","anchor":"fine-grained-signals","snippet":"Alpine re-runs an element's effects when the data it touched changes. Summit runs on a signal engine that tracks reads at the level of individual values. When you update one property, the only expressions that re-run are","text":"fine-grained signals alpine re-runs an element's effects when the data it touched changes. summit runs on a signal engine that tracks reads at the level of individual values. when you update one property, the only expressions that re-run are the ones that actually read that property, and each updates just its own text node or attribute. there is no subtree to re-evaluate. one practical upshot is getters. a getter you define in s-data is a cached computed: it recomputes only when one of its own dependencies changes, and reading it several times in one render does the work once. the same getter in alpine re-runs on every read. see how reactivity works for the full picture."},{"page":"Migrating from Alpine","category":"Advanced","heading":"CSP-safe with the standard build","slug":"advanced-migrating","anchor":"csp-safe-with-the-standard-build","snippet":"Alpine's default build compiles expressions with new Function, so a strict Content-Security-Policy needs Alpine's separate CSP build (which restricts what expressions may do). Summit interprets expressions with its own e","text":"csp-safe with the standard build alpine's default build compiles expressions with new function, so a strict content-security-policy needs alpine's separate csp build (which restricts what expressions may do). summit interprets expressions with its own engine and never calls eval or new function, so a policy like script-src 'self' works with the one and only build, and directive expressions still support a broad slice of javascript. the details, including the exact supported syntax and the global allowlist, are in the csp-safe evaluator."},{"page":"Migrating from Alpine","category":"Advanced","heading":"$watch returns an unwatch function and will not loop","slug":"advanced-migrating","anchor":"watch-returns-an-unwatch-function-and-will-not-loop","snippet":"In Summit, $watch returns a function that stops the watcher, and the callback fires only on an actual change (compared with Object.is), skipping the initial value. That means mutating the watched value inside its own cal","text":"$watch returns an unwatch function and will not loop in summit, $watch returns a function that stops the watcher, and the callback fires only on an actual change (compared with object.is), skipping the initial value. that means mutating the watched value inside its own callback cannot spin into an infinite loop, and you can tear a watcher down when you no longer need it."},{"page":"Migrating from Alpine","category":"Advanced","heading":"Keyed s-for reconciliation","slug":"advanced-migrating","anchor":"keyed-s-for-reconciliation","snippet":"Give s-for a :key and Summit reconciles the list by key. Matching rows are reused and moved rather than destroyed and recreated, so focus, scroll position, input values, and any per-row state survive reordering and inser","text":"keyed s-for reconciliation give s-for a :key and summit reconciles the list by key. matching rows are reused and moved rather than destroyed and recreated, so focus, scroll position, input values, and any per-row state survive reordering and insertion. this is the area where alpine struggles most on large or reordering lists."},{"page":"Migrating from Alpine","category":"Advanced","heading":"s-if works on any element","slug":"advanced-migrating","anchor":"s-if-works-on-any-element","snippet":"Alpine's x-if must wrap a <template. Summit's s-if works directly on any element. Summit drops a comment anchor where the element was and uses the element itself, minus its s-if attribute, as the blueprint it clones in a","text":"s-if works on any element alpine's x-if must wrap a <template. summit's s-if works directly on any element. summit drops a comment anchor where the element was and uses the element itself, minus its s-if attribute, as the blueprint it clones in and out. you can still use a <template if you prefer, and you should when the content is more than a single root element."},{"page":"Migrating from Alpine","category":"Advanced","heading":"The s-model modifier set","slug":"advanced-migrating","anchor":"the-s-model-modifier-set","snippet":"s-model handles text, number, range, checkbox (boolean or array), radio, and single or multiple selects, with these modifiers: .lazy, .change, .blur, .number, .boolean, .debounce, .throttle, and .fill. A few are worth ca","text":"the s-model modifier set s-model handles text, number, range, checkbox (boolean or array), radio, and single or multiple selects, with these modifiers: .lazy, .change, .blur, .number, .boolean, .debounce, .throttle, and .fill. a few are worth calling out for alpine users: - .fill seeds the model from the control's own initial value when the model is empty, so server-rendered form values become the starting state. - .boolean coerces the input to a real boolean. - .debounce and .throttle accept a duration, for example s-model.debounce.500ms. the bound value is also exposed on the element as el.summitmodel = { get, set }, which makes it straightforward to build custom input components that participate in s-model."},{"page":"Migrating from Alpine","category":"Advanced","heading":"A note on load order","slug":"advanced-migrating","anchor":"a-note-on-load-order","snippet":"Alpine is sensitive to when you register directives and plugins relative to alpine:init. Summit's registries are timing-safe: registration order does not matter and a later registration overrides an earlier one of the sa","text":"a note on load order alpine is sensitive to when you register directives and plugins relative to alpine:init. summit's registries are timing-safe: registration order does not matter and a later registration overrides an earlier one of the same name. summit dispatches summit:init on document just before it initializes the page and summit:initialized when it is done, so register your plugins before start, or inside a summit:init listener. see extending summit for the full pattern."},{"page":"How reactivity works","category":"Advanced","heading":"How reactivity works","slug":"advanced-reactivity","anchor":"","snippet":"Everything reactive in Summit rests on one small engine. A handful of primitives, a few hundred lines, decide what re-runs when a value changes. This page walks that engine end to end so you can reason about updates, wri","text":"how reactivity works everything reactive in summit rests on one small engine. a handful of primitives, a few hundred lines, decide what re-runs when a value changes. this page walks that engine end to end so you can reason about updates, write your own effects, and understand exactly why a directive fires when it does. if you just want to use signals, computeds, and effects, the reactivity reference pages cover the public api. this page is about the machinery underneath them."},{"page":"How reactivity works","category":"Advanced","heading":"The signal, the atom of state","slug":"advanced-reactivity","anchor":"the-signal-the-atom-of-state","snippet":"A signal holds one value. Reading it subscribes whoever is currently watching. Writing it wakes those watchers, but only if the value actually changed. Internally each signal owns a dep, a set of the effects that read it","text":"the signal, the atom of state a signal holds one value. reading it subscribes whoever is currently watching. writing it wakes those watchers, but only if the value actually changed. internally each signal owns a dep, a set of the effects that read it. a read calls track(dep), which adds the active effect to that set. a write compares the new value to the old one with object.is and, only if they differ, calls trigger(dep) to wake every effect in the set. setting a signal to its current value does nothing, so no downstream work happens for a no-op write. .peek() skips track, so you can read a value without becoming dependent on it."},{"page":"How reactivity works","category":"Advanced","heading":"Effects and dependency tracking","slug":"advanced-reactivity","anchor":"effects-and-dependency-tracking","snippet":"An effect is a function that re-runs whenever any value it read last time changes. It is the other half of the signal: signals are read inside effects, and effects are what signals wake. The link is built by running the ","text":"effects and dependency tracking an effect is a function that re-runs whenever any value it read last time changes. it is the other half of the signal: signals are read inside effects, and effects are what signals wake. the link is built by running the function. before an effect runs, summit sets it as the current active effect. any signal read during that run calls track, which records the dependency in both directions: the effect is added to the signal's dep set, and the dep is pushed onto the effect's own deps list. dependencies are re-collected on every run. the first thing an effect does when it re-runs is cleanupdeps, which unsubscribes it from every dep it currently holds. it then runs the function and re-subscribes only to what it reads this time. that is why tracking is fine-grained and dynamic: if an effect takes a different branch and stops reading a value, it stops depending on it. nothing you no longer read can wake you. because a dep is one set per value, a change wakes only the effects that read that exact value. when you click a button that increments count, the only effects that re-run are the ones that read count. every other text node, attribute, and list on the page is untouched."},{"page":"How reactivity works","category":"Advanced","heading":"The active-effect stack","slug":"advanced-reactivity","anchor":"the-active-effect-stack","snippet":"Only one effect can be \"active\" at a time, but effects nest: an effect can run code that creates or runs another effect. Summit tracks this with a stack. When an effect starts it is pushed on and becomes active; when it ","text":"the active-effect stack only one effect can be \"active\" at a time, but effects nest: an effect can run code that creates or runs another effect. summit tracks this with a stack. when an effect starts it is pushed on and becomes active; when it finishes it is popped and the previous effect on the stack becomes active again. reads always attribute to the effect at the top of the stack, so a nested effect captures its own dependencies without stealing the outer effect's. the stack also powers the self-write guard. an effect that both reads and writes the same value could otherwise trigger itself forever. two checks prevent that. first, if an effect is already on the stack when it is asked to run again, the re-entrant call returns immediately. second, trigger never re-runs the effect that is currently active. so an effect can safely write a value it also reads without spinning into an infinite loop."},{"page":"How reactivity works","category":"Advanced","heading":"Computed values are cached","slug":"advanced-reactivity","anchor":"computed-values-are-cached","snippet":"A computed is a derived value that memoizes its result. Unlike a plain getter, which recomputes on every read, a computed recomputes only when one of its own dependencies changes, and it stays lazy: the getter does not r","text":"computed values are cached a computed is a derived value that memoizes its result. unlike a plain getter, which recomputes on every read, a computed recomputes only when one of its own dependencies changes, and it stays lazy: the getter does not run until something reads the computed. a computed is built on a lazy effect plus a dirty flag. reading the computed runs the getter only when dirty is set, stores the result, and clears the flag. when a dependency changes, the computed's scheduler marks it dirty again and notifies the computed's own subscribers so they re-read on demand. this is why a computed is both a consumer (it subscribes to whatever its getter reads) and a producer (it has its own dep that effects can subscribe to). the getters you write in s-data are backed by this, so reading a derived value twice in one render does the work at most once."},{"page":"How reactivity works","category":"Advanced","heading":"Batching writes","slug":"advanced-reactivity","anchor":"batching-writes","snippet":"Sometimes you change several values at once and want dependent effects to run a single time at the end, not once per write. That is what batch does. While a batch is open, triggered effects are collected into a pending s","text":"batching writes sometimes you change several values at once and want dependent effects to run a single time at the end, not once per write. that is what batch does. while a batch is open, triggered effects are collected into a pending set instead of running. when the outermost batch closes, that set is flushed once. batches nest, and only the outermost flush runs anything, so helper functions that each open a batch compose cleanly."},{"page":"How reactivity works","category":"Advanced","heading":"The scheduler and the DOM","slug":"advanced-reactivity","anchor":"the-scheduler-and-the-dom","snippet":"Batching groups writes you make yourself. The scheduler solves a different problem: keeping DOM updates cheap regardless of how the writes arrive. Plain effects run synchronously the moment a dependency changes. DOM-faci","text":"the scheduler and the dom batching groups writes you make yourself. the scheduler solves a different problem: keeping dom updates cheap regardless of how the writes arrive. plain effects run synchronously the moment a dependency changes. dom-facing effects do not. they are created with a scheduler, so instead of re-running inline they queue themselves onto a microtask. the queue is a set, so an effect that is triggered many times in one tick is only queued once, and the whole queue flushes together on the next microtask. a burst of state changes therefore produces a single coalesced dom update, even without an explicit batch. nexttick sits on top of this queue. it resolves after the current flush, which is how you read the freshly updated dom. the primitive that ties effects to this scheduler is domeffect. it creates a lazy effect whose scheduler enqueues it, runs it once immediately to establish its dependencies and paint the first result, and returns a disposer. run once now, re-run on a microtask forever after, until stopped."},{"page":"How reactivity works","category":"Advanced","heading":"How directives sit on top","slug":"advanced-reactivity","anchor":"how-directives-sit-on-top","snippet":"Every reactive directive is a domEffect. When a directive calls utils.effect(fn), Summit wraps fn in a domEffect and registers its disposer as element cleanup. That single mechanism explains the whole system: - s-text ru","text":"how directives sit on top every reactive directive is a domeffect. when a directive calls utils.effect(fn), summit wraps fn in a domeffect and registers its disposer as element cleanup. that single mechanism explains the whole system: - s-text runs an effect that sets textcontent from an expression. the effect reads whatever the expression reads, so it re-runs when any of those values change, and updates only that one text node. - s-show, :bind, and s-model are the same shape: an effect that reads state and writes one piece of the dom. - s-if and s-for run effects that read a condition or a list and add or remove nodes in response. the component scope itself, the object you pass to s-data, is wrapped in a reactive proxy. reading count inside a directive expression goes through that proxy, which calls track for the count property, subscribing the directive's effect to exactly that property. writing count calls trigger for that property, waking exactly the directives that read it. when the element is removed, summit runs its cleanups, which stop those effects and unsubscribe them from every dep."},{"page":"How reactivity works","category":"Advanced","heading":"Reactive objects","slug":"advanced-reactivity","anchor":"reactive-objects","snippet":"The bridge between the low-level signal API and the object ergonomics of s-data is reactive. It wraps a plain object or array in a proxy that tracks property reads and triggers on writes, so this.open = true inside a met","text":"reactive objects the bridge between the low-level signal api and the object ergonomics of s-data is reactive. it wraps a plain object or array in a proxy that tracks property reads and triggers on writes, so this.open = true inside a method just works. reactivity is deep and lazy: nested plain objects and arrays are wrapped the first time you access them. only plain objects and arrays are wrapped, though. class instances, dom nodes, date, map, regexp, and other host objects are returned untouched, so storing a dom element in reactive data never breaks it. the proxy is cached per object and idempotent, so wrapping the same object twice returns the same proxy, and toraw unwraps it again when you need the plain value."},{"page":"How reactivity works","category":"Advanced","heading":"Where to go next","slug":"advanced-reactivity","anchor":"where-to-go-next","snippet":"- signal and computed for the value primitives. - effect for the tracking primitive, and reactive for deep reactive objects. - batch and nextTick for controlling when work runs.","text":"where to go next - signal and computed for the value primitives. - effect for the tracking primitive, and reactive for deep reactive objects. - batch and nexttick for controlling when work runs."},{"page":"Accordion","category":"Components","heading":"Accordion","slug":"comp-accordion","anchor":"","snippet":"An accordion stacks sections that expand and collapse in place. Each header is a real <button, the chevron rotates through a class, and the panel below is revealed with s-show. The classic behavior is one section open at","text":"accordion an accordion stacks sections that expand and collapse in place. each header is a real <button, the chevron rotates through a class, and the panel below is revealed with s-show. the classic behavior is one section open at a time, which takes a single string of state."},{"page":"Accordion","category":"Components","heading":"One open at a time","slug":"comp-accordion","anchor":"one-open-at-a-time","snippet":"Track which item is open as a string. Each trigger toggles: click the open one and it sets open back to empty; click a closed one and it takes over. Because only one value can match, opening a section closes whatever was","text":"one open at a time track which item is open as a string. each trigger toggles: click the open one and it sets open back to empty; click a closed one and it takes over. because only one value can match, opening a section closes whatever was open before."},{"page":"Accordion","category":"Components","heading":"Allowing multiple open","slug":"comp-accordion","anchor":"allowing-multiple-open","snippet":"To let several sections stay open at once, track an array of open ids instead of a single string. Each trigger toggles its own membership: if the id is already in the array, splice it out; otherwise push it in. The class","text":"allowing multiple open to let several sections stay open at once, track an array of open ids instead of a single string. each trigger toggles its own membership: if the id is already in the array, splice it out; otherwise push it in. the class check and the panel both test with includes."},{"page":"Accordion","category":"Components","heading":"Notes","slug":"comp-accordion","anchor":"notes","snippet":"- Every trigger is a real button, so it is keyboard reachable and toggles with Enter or Space without extra wiring. - The .s-chevron rotates on its own through the is-open class in components.css; you only bind the class","text":"notes - every trigger is a real button, so it is keyboard reachable and toggles with enter or space without extra wiring. - the .s-chevron rotates on its own through the is-open class in components.css; you only bind the class, never the transform. - start open at an id (or seed the array) to have a section expanded on first render, or set it to '' (or []) to begin fully collapsed. for swapping between panels rather than stacking them, see tabs. the whole set is on the overview."},{"page":"Alert","category":"Components","heading":"Alert","slug":"comp-alert","anchor":"","snippet":"An alert is an inline banner that gives the reader feedback in place: a helpful note, a success confirmation, a warning, or an error. It sits in the flow of the page rather than floating over it, which makes it the right","text":"alert an alert is an inline banner that gives the reader feedback in place: a helpful note, a success confirmation, a warning, or an error. it sits in the flow of the page rather than floating over it, which makes it the right choice for messages tied to a specific spot, like a form section or a settings panel. each variant pairs an icon with a title and a short description."},{"page":"Alert","category":"Components","heading":"Anatomy","slug":"comp-alert","anchor":"anatomy","snippet":"An alert is a flex row of .s-alert plus a variant class: - .s-alert-info, .s-alert-success, .s-alert-warning, or .s-alert-danger set the background tint, border, and icon color from the semantic palette. - .s-alert-icon ","text":"anatomy an alert is a flex row of .s-alert plus a variant class: - .s-alert-info, .s-alert-success, .s-alert-warning, or .s-alert-danger set the background tint, border, and icon color from the semantic palette. - .s-alert-icon on an inline <svg sizes and colors the leading icon. use stroke=\"currentcolor\" so it inherits the variant color. - .s-alert-title is the bold headline and .s-alert-desc is the muted body. wrap them together in a <div so they stack beside the icon. leave off the variant class for a neutral, surface-colored alert. you can also drop the title and keep a single .s-alert-desc for a one-line note."},{"page":"Alert","category":"Components","heading":"Notes","slug":"comp-alert","anchor":"notes","snippet":"- Pick the variant by meaning, not color. Danger is for errors and destructive outcomes, warning for things that need attention soon. - The icon is decorative here because the title carries the message. If an alert has n","text":"notes - pick the variant by meaning, not color. danger is for errors and destructive outcomes, warning for things that need attention soon. - the icon is decorative here because the title carries the message. if an alert has no text title, add an aria-label to convey its intent. - alerts stay in the layout. for transient, stacked messages that appear and auto-dismiss, use a toast; for status labels, use a badge. see the whole component set on the overview."},{"page":"Avatar","category":"Components","heading":"Avatar","slug":"comp-avatar","anchor":"","snippet":"An avatar is a small, round token that stands in for a person or account. Give it initials and it renders a tinted circle with centered text; give it an <img and the photo fills the circle, cropped to a cover fit. Three ","text":"avatar an avatar is a small, round token that stands in for a person or account. give it initials and it renders a tinted circle with centered text; give it an <img and the photo fills the circle, cropped to a cover fit. three sizes and a stacked group cover most of what a user list, comment thread, or header bar needs."},{"page":"Avatar","category":"Components","heading":"Sizes","slug":"comp-avatar","anchor":"sizes","snippet":"The base avatar is 40px. Add .s-avatar-sm for a compact 30px token or .s-avatar-lg for a 56px one. The font scales with each size, so initials stay centered and legible.","text":"sizes the base avatar is 40px. add .s-avatar-sm for a compact 30px token or .s-avatar-lg for a 56px one. the font scales with each size, so initials stay centered and legible."},{"page":"Avatar","category":"Components","heading":"Images","slug":"comp-avatar","anchor":"images","snippet":"Put an <img inside .s-avatar and it covers the circle. The wrapper clips the photo to a round shape, so any square or rectangular source works.","text":"images put an <img inside .s-avatar and it covers the circle. the wrapper clips the photo to a round shape, so any square or rectangular source works."},{"page":"Avatar","category":"Components","heading":"Stacked group","slug":"comp-avatar","anchor":"stacked-group","snippet":"Wrap several avatars in .s-avatar-group and they overlap with a ring in the page background between them, the classic \"who is here\" cluster. A trailing avatar with a count reads as an overflow.","text":"stacked group wrap several avatars in .s-avatar-group and they overlap with a ring in the page background between them, the classic \"who is here\" cluster. a trailing avatar with a count reads as an overflow."},{"page":"Avatar","category":"Components","heading":"Notes","slug":"comp-avatar","anchor":"notes","snippet":"- Keep initials to one or two characters so they stay centered inside the circle. - Always give the <img a descriptive alt, or an empty alt=\"\" when the name already appears next to the avatar and the image is decorative.","text":"notes - keep initials to one or two characters so they stay centered inside the circle. - always give the <img a descriptive alt, or an empty alt=\"\" when the name already appears next to the avatar and the image is decorative. - the ring around grouped avatars comes from the page background color. place a group on a plain surface so the separation reads clearly. pair avatars with cards and badges, or browse the full set on the overview."},{"page":"Badge & Tag","category":"Components","heading":"Badge & Tag","slug":"comp-badge","anchor":"","snippet":"Badges are small, uppercase labels for status and counts: a build that passed, a plan tier, an item count on a menu. Tags are their rounded, softer cousin, sized for user-entered keywords and built to carry a remove butt","text":"badge & tag badges are small, uppercase labels for status and counts: a build that passed, a plan tier, an item count on a menu. tags are their rounded, softer cousin, sized for user-entered keywords and built to carry a remove button. both are plain markup, and the tag's button is where a little summit state turns a static list into an editable one."},{"page":"Badge & Tag","category":"Components","heading":"Badge variants","slug":"comp-badge","anchor":"badge-variants","snippet":"The base .s-badge uses your accent tint. Add one modifier to change its weight or meaning: - .s-badge-solid fills with the accent color for the strongest emphasis. - .s-badge-outline is a quiet, bordered label. - .s-badg","text":"badge variants the base .s-badge uses your accent tint. add one modifier to change its weight or meaning: - .s-badge-solid fills with the accent color for the strongest emphasis. - .s-badge-outline is a quiet, bordered label. - .s-badge-info, .s-badge-success, .s-badge-warning, and .s-badge-danger map to the semantic feedback palette, the same colors the alert component uses."},{"page":"Badge & Tag","category":"Components","heading":"Removable tags","slug":"comp-badge","anchor":"removable-tags","snippet":"A .s-tag is a pill with an inner <button for removal. Bind the list to state with s-for, key each tag, and let the button splice its own item out of the array. Because the array is reactive, the tag disappears the moment","text":"removable tags a .s-tag is a pill with an inner <button for removal. bind the list to state with s-for, key each tag, and let the button splice its own item out of the array. because the array is reactive, the tag disappears the moment you click."},{"page":"Badge & Tag","category":"Components","heading":"Notes","slug":"comp-badge","anchor":"notes","snippet":"- Give the remove button an aria-label so screen readers announce what it does, since the visible content is just a times sign. - The :key=\"tag\" keeps each pill matched to its value across removals, so the right one leav","text":"notes - give the remove button an aria-label so screen readers announce what it does, since the visible content is just a times sign. - the :key=\"tag\" keeps each pill matched to its value across removals, so the right one leaves even when earlier tags are gone. use a unique id if your tags can repeat. - badges have no interactive behavior; they are labels. for dismissible banners, reach for an alert or a toast instead. see the whole component set on the overview."},{"page":"Breadcrumb","category":"Components","heading":"Breadcrumb","slug":"comp-breadcrumb","anchor":"","snippet":"A breadcrumb shows where the current page sits in your hierarchy and lets people step back up it. It is pure markup: a <nav labeled for assistive tech, a chain of links, and a plain .s-sep between each one. The last item","text":"breadcrumb a breadcrumb shows where the current page sits in your hierarchy and lets people step back up it. it is pure markup: a <nav labeled for assistive tech, a chain of links, and a plain .s-sep between each one. the last item is the current page, so it is text rather than a link."},{"page":"Breadcrumb","category":"Components","heading":"The trail","slug":"comp-breadcrumb","anchor":"the-trail","snippet":"Each ancestor is a normal <a, separated by a <span class=\"s-sep\". The final crumb carries aria-current=\"page\" and no link, which both styles it as the current location and tells screen readers they have arrived.","text":"the trail each ancestor is a normal <a, separated by a <span class=\"s-sep\". the final crumb carries aria-current=\"page\" and no link, which both styles it as the current location and tells screen readers they have arrived."},{"page":"Breadcrumb","category":"Components","heading":"Notes","slug":"comp-breadcrumb","anchor":"notes","snippet":"- Mark the last crumb with aria-current=\"page\". It is the one visual and assistive-tech signal that this crumb is where you are, and the stylesheet keys its stronger color off that attribute. - Give the <nav an aria-labe","text":"notes - mark the last crumb with aria-current=\"page\". it is the one visual and assistive-tech signal that this crumb is where you are, and the stylesheet keys its stronger color off that attribute. - give the <nav an aria-label such as breadcrumb so it is announced as a distinct landmark separate from your main navigation. - the separator is decorative. keeping it in its own .s-sep span leaves the links clean and lets you swap the character (a slash, a chevron) in one place. - the trail is static markup, so it drops into any page as-is. to render it from a route array, wrap the crumbs in a s-for and mark the final item with aria-current. to move through pages of results rather than up a hierarchy, see pagination. the whole set is on the overview."},{"page":"Button","category":"Components","heading":"Button","slug":"comp-button","anchor":"","snippet":"Buttons trigger actions. Everything starts from one base class, .s-btn, and you add modifiers for variant, size, and shape. Because behavior lives on the element, you can wire any button up with the same s- directives yo","text":"button buttons trigger actions. everything starts from one base class, .s-btn, and you add modifiers for variant, size, and shape. because behavior lives on the element, you can wire any button up with the same s- directives you already use."},{"page":"Button","category":"Components","heading":"Variants","slug":"comp-button","anchor":"variants","snippet":"The base .s-btn is the solid, high-emphasis button. Add one modifier class to change its emphasis. Use .s-btn-danger for destructive actions so the intent reads at a glance.","text":"variants the base .s-btn is the solid, high-emphasis button. add one modifier class to change its emphasis. use .s-btn-danger for destructive actions so the intent reads at a glance."},{"page":"Button","category":"Components","heading":"Sizes","slug":"comp-button","anchor":"sizes","snippet":".s-btn-sm and .s-btn-lg adjust height and padding. Without either, a button uses the default size. Add .s-btn-block to stretch a button to the full width of its container, which is handy at the end of a form.","text":"sizes .s-btn-sm and .s-btn-lg adjust height and padding. without either, a button uses the default size. add .s-btn-block to stretch a button to the full width of its container, which is handy at the end of a form."},{"page":"Button","category":"Components","heading":"Icon buttons","slug":"comp-button","anchor":"icon-buttons","snippet":".s-btn-icon makes a square button sized for a single glyph. Drop an inline SVG inside and give the button an aria-label, since there is no visible text to name it.","text":"icon buttons .s-btn-icon makes a square button sized for a single glyph. drop an inline svg inside and give the button an aria-label, since there is no visible text to name it."},{"page":"Button","category":"Components","heading":"Groups","slug":"comp-button","anchor":"groups","snippet":"Wrap two or more buttons in .s-btn-group to join them into a single segmented control. The group squares off the inner corners and collapses the shared borders for you.","text":"groups wrap two or more buttons in .s-btn-group to join them into a single segmented control. the group squares off the inner corners and collapses the shared borders for you."},{"page":"Button","category":"Components","heading":"Disabled","slug":"comp-button","anchor":"disabled","snippet":"The native disabled attribute dims a button and removes it from the tab order. Bind it reactively with :disabled to gate an action on state.","text":"disabled the native disabled attribute dims a button and removes it from the tab order. bind it reactively with :disabled to gate an action on state."},{"page":"Button","category":"Components","heading":"Accessibility","slug":"comp-button","anchor":"accessibility","snippet":"Always use a real <button element so keyboard activation, focus, and the disabled state work without extra code. Every variant shows a visible focus ring on :focus-visible. Give icon-only buttons an aria-label so assisti","text":"accessibility always use a real <button element so keyboard activation, focus, and the disabled state work without extra code. every variant shows a visible focus ring on :focus-visible. give icon-only buttons an aria-label so assistive technology can name them, and label a .s-btn-group with role=\"group\" and an aria-label describing the set. for handling clicks and keyboard shortcuts, see s-on; to pair a button with a field, see input."},{"page":"Card","category":"Components","heading":"Card","slug":"comp-card","anchor":"","snippet":"A card is a bordered surface that groups related content into one block. Reach for it whenever a chunk of the page deserves its own edges: a settings panel, a pricing tier, a summary. It is pure markup, so there is nothi","text":"card a card is a bordered surface that groups related content into one block. reach for it whenever a chunk of the page deserves its own edges: a settings panel, a pricing tier, a summary. it is pure markup, so there is nothing to wire up. you compose a header, a body, and a footer, and use only the parts you need."},{"page":"Card","category":"Components","heading":"Anatomy","slug":"comp-card","anchor":"anatomy","snippet":"A card is a .s-card wrapper around three optional regions, each with its own padding so they line up without extra spacing rules. - .s-card-header holds the .s-card-title and an optional .s-card-desc. - .s-card-body is t","text":"anatomy a card is a .s-card wrapper around three optional regions, each with its own padding so they line up without extra spacing rules. - .s-card-header holds the .s-card-title and an optional .s-card-desc. - .s-card-body is the main content area. - .s-card-footer lays out actions in a row with a gap, ready for buttons. the wrapper clips its corners with overflow: hidden, so a full-bleed image or a colored bar as the first child follows the rounded edge cleanly."},{"page":"Card","category":"Components","heading":"Body-only card","slug":"comp-card","anchor":"body-only-card","snippet":"Drop the header and footer when all you need is a framed block of content.","text":"body-only card drop the header and footer when all you need is a framed block of content."},{"page":"Card","category":"Components","heading":"Notes","slug":"comp-card","anchor":"notes","snippet":"- The card sets a width of whatever it sits in, so control its size from the parent layout (a grid, a flex row, or a max-width like the demos above). - The title renders as an h3 here, but the class carries the styling. ","text":"notes - the card sets a width of whatever it sits in, so control its size from the parent layout (a grid, a flex row, or a max-width like the demos above). - the title renders as an h3 here, but the class carries the styling. use the heading level that fits your document outline. - footer actions align to the start. to push them to the right, add your own style=\"justify-content:flex-end\" or a utility class. pair cards with badges for status and avatars for people. see the full set on the overview."},{"page":"Checkbox & Radio","category":"Components","heading":"Checkbox & Radio","slug":"comp-checkbox","anchor":"","snippet":"Checkboxes and radios wrap a real <input in a styled label, so you get a custom look without giving up native behavior. The input stays in the markup and carries the state; the .s-box next to it is only decoration. Bind ","text":"checkbox & radio checkboxes and radios wrap a real <input in a styled label, so you get a custom look without giving up native behavior. the input stays in the markup and carries the state; the .s-box next to it is only decoration. bind any of them with s-model and the checked state flows both ways."},{"page":"Checkbox & Radio","category":"Components","heading":"A single checkbox","slug":"comp-checkbox","anchor":"a-single-checkbox","snippet":"Point s-model at a boolean and the checkbox toggles it. The <input is visually hidden but still present, and the .s-box renders the tick when the value is true.","text":"a single checkbox point s-model at a boolean and the checkbox toggles it. the <input is visually hidden but still present, and the .s-box renders the tick when the value is true."},{"page":"Checkbox & Radio","category":"Components","heading":"A group bound to an array","slug":"comp-checkbox","anchor":"a-group-bound-to-an-array","snippet":"When several checkboxes share one s-model and each carries a value, Summit binds them to an array. Checking a box adds its value; unchecking removes it. The order follows the order the boxes appear in the markup.","text":"a group bound to an array when several checkboxes share one s-model and each carries a value, summit binds them to an array. checking a box adds its value; unchecking removes it. the order follows the order the boxes appear in the markup."},{"page":"Checkbox & Radio","category":"Components","heading":"A radio group","slug":"comp-checkbox","anchor":"a-radio-group","snippet":"Radios use .s-radio-opt, which rounds the .s-box and fills it with a dot when selected. Give every radio in the group the same s-model and a distinct value; the model holds the one chosen value.","text":"a radio group radios use .s-radio-opt, which rounds the .s-box and fills it with a dot when selected. give every radio in the group the same s-model and a distinct value; the model holds the one chosen value."},{"page":"Checkbox & Radio","category":"Components","heading":"Accessibility","slug":"comp-checkbox","anchor":"accessibility","snippet":"The styled box is purely visual. Because a real <input type=\"checkbox\" or <input type=\"radio\" stays in the DOM, you keep native keyboard support out of the box: focus with Tab, toggle a checkbox with Space, and move with","text":"accessibility the styled box is purely visual. because a real <input type=\"checkbox\" or <input type=\"radio\" stays in the dom, you keep native keyboard support out of the box: focus with tab, toggle a checkbox with space, and move within a radio group with the arrow keys. screen readers announce the control's role and checked state, and wrapping each control in its <label means the visible text is the accessible name, so no extra aria is needed. grouping related radios or checkboxes in a <fieldset with a <legend gives the set a shared label. for an on/off setting, reach for the switch instead. see forms for how s-model handles every control type."},{"page":"Dialog","category":"Components","heading":"Dialog","slug":"comp-dialog","anchor":"","snippet":"A dialog interrupts the page to ask for a decision or hold a short, focused task. It draws an overlay across the page, floats a panel in the center, and traps the user's attention until they act or dismiss it. Everything","text":"dialog a dialog interrupts the page to ask for a decision or hold a short, focused task. it draws an overlay across the page, floats a panel in the center, and traps the user's attention until they act or dismiss it. everything is driven by one piece of state: a boolean that says whether the dialog is open. a button flips it on, and the overlay, the close button, and the escape key flip it back off."},{"page":"Dialog","category":"Components","heading":"Opening and closing","slug":"comp-dialog","anchor":"opening-and-closing","snippet":"The whole component lives on the open boolean in s-data. The trigger button sets it to true, and every path out of the dialog sets it back to false. The overlay and panel sit inside a <template s-if=\"open\", so they are a","text":"opening and closing the whole component lives on the open boolean in s-data. the trigger button sets it to true, and every path out of the dialog sets it back to false. the overlay and panel sit inside a <template s-if=\"open\", so they are added to the page only while the dialog is open and removed cleanly when it closes. using s-if here, rather than s-show, means the markup does not sit hidden in the dom between openings. there are three ways to close it, and all of them write the same state: - the overlay: @click=\"open = false\" on .s-overlay closes when the user clicks the dimmed area behind the panel. - the escape key: @keydown.escape.window=\"open = false\" listens on the window so the key works no matter where focus sits. - the close button: the .s-dialog-close in the corner, plus the cancel button in the footer."},{"page":"Dialog","category":"Components","heading":"The pieces","slug":"comp-dialog","anchor":"the-pieces","snippet":"The panel is built from a small set of slots you fill in: - .s-overlay is the fixed, dimmed backdrop. It also acts as the click-to-close surface. - .s-dialog is the centered panel. Give it role=\"dialog\" and aria-modal=\"t","text":"the pieces the panel is built from a small set of slots you fill in: - .s-overlay is the fixed, dimmed backdrop. it also acts as the click-to-close surface. - .s-dialog is the centered panel. give it role=\"dialog\" and aria-modal=\"true\" so assistive tech announces it as a modal. - .s-dialog-header wraps the .s-dialog-title. - .s-dialog-close is the corner dismiss button. label it with aria-label since its only content is a glyph. - .s-dialog-body holds the message or form. - .s-dialog-footer right-aligns the actions. pair a neutral button for cancel with a solid or danger one for confirm."},{"page":"Dialog","category":"Components","heading":"Side panel variant","slug":"comp-dialog","anchor":"side-panel-variant","snippet":"Swap .s-dialog for .s-drawer and the panel docks to the right edge and runs the full height of the viewport, which suits filters, details, and longer forms. The overlay, the state, and every close path stay exactly the s","text":"side panel variant swap .s-dialog for .s-drawer and the panel docks to the right edge and runs the full height of the viewport, which suits filters, details, and longer forms. the overlay, the state, and every close path stay exactly the same; only the panel class changes."},{"page":"Dialog","category":"Components","heading":"Rendering in a real app","slug":"comp-dialog","anchor":"rendering-in-a-real-app","snippet":"The demo above keeps the overlay next to its trigger so the example stays self-contained. In a real layout that is risky: an ancestor with overflow: hidden can clip the panel, and a local stacking context can push it beh","text":"rendering in a real app the demo above keeps the overlay next to its trigger so the example stays self-contained. in a real layout that is risky: an ancestor with overflow: hidden can clip the panel, and a local stacking context can push it behind other content. the fix is to render the dialog at the end of <body, clear of any parent, with s-teleport: the panel is authored inside the component, so open and the close handlers keep working, but it lands at the end of the page where nothing can clip or restack it."},{"page":"Dialog","category":"Components","heading":"Accessibility","slug":"comp-dialog","anchor":"accessibility","snippet":"Mark the panel with role=\"dialog\" and aria-modal=\"true\" so screen readers announce it as a modal and treat the content behind it as inert. Point the panel at its title with aria-labelledby (pair the .s-dialog-title with ","text":"accessibility mark the panel with role=\"dialog\" and aria-modal=\"true\" so screen readers announce it as a modal and treat the content behind it as inert. point the panel at its title with aria-labelledby (pair the .s-dialog-title with an id, which the $id magic can generate) so the dialog has an accessible name. the escape handler is bound with .window, so the key closes the dialog from anywhere, which matches the behavior users expect. give the corner close button an aria-label, since its &times; glyph is not a readable name. when you open a dialog, move focus into it and return focus to the trigger on close so keyboard users are not stranded. for a menu of actions rather than a blocking decision, reach for the dropdown menu."},{"page":"Input","category":"Components","heading":"Input","slug":"comp-input","anchor":"","snippet":"Inputs collect a line or a block of text. Apply .s-input to any text-like <input and .s-textarea to a <textarea, then bind the value with s-model to keep state and control in sync.","text":"input inputs collect a line or a block of text. apply .s-input to any text-like <input and .s-textarea to a <textarea, then bind the value with s-model to keep state and control in sync."},{"page":"Input","category":"Components","heading":"A full field","slug":"comp-input","anchor":"a-full-field","snippet":"Wrap a label, control, and help text in .s-field for consistent spacing. Use .s-label for the label and .s-help for the hint below. Mark required fields with a .s-req asterisk inside the label.","text":"a full field wrap a label, control, and help text in .s-field for consistent spacing. use .s-label for the label and .s-help for the hint below. mark required fields with a .s-req asterisk inside the label."},{"page":"Input","category":"Components","heading":"Input groups","slug":"comp-input","anchor":"input-groups","snippet":".s-input-group sits an .s-addon flush against an input to show a prefix such as a protocol, currency symbol, or unit. The group squares off the touching corners for you.","text":"input groups .s-input-group sits an .s-addon flush against an input to show a prefix such as a protocol, currency symbol, or unit. the group squares off the touching corners for you."},{"page":"Input","category":"Components","heading":"Validation state","slug":"comp-input","anchor":"validation-state","snippet":"Add is-invalid to an input to switch its border to the danger color, and show an .s-error message in place of the help text. Bind both to state so the error appears only when the value is actually wrong.","text":"validation state add is-invalid to an input to switch its border to the danger color, and show an .s-error message in place of the help text. bind both to state so the error appears only when the value is actually wrong."},{"page":"Input","category":"Components","heading":"Textarea","slug":"comp-input","anchor":"textarea","snippet":".s-textarea styles a multi-line field. It grows vertically and can be resized by the user. Bind it with s-model the same way as a single-line input.","text":"textarea .s-textarea styles a multi-line field. it grows vertically and can be resized by the user. bind it with s-model the same way as a single-line input."},{"page":"Input","category":"Components","heading":"Accessibility","slug":"comp-input","anchor":"accessibility","snippet":"Pair every control with a <label by matching the label's for to the input's id, so clicking the label focuses the field. When ids are generated at runtime, reach for the $id magic. The .s-req asterisk is decorative, so k","text":"accessibility pair every control with a <label by matching the label's for to the input's id, so clicking the label focuses the field. when ids are generated at runtime, reach for the $id magic. the .s-req asterisk is decorative, so keep the field's real requirement in the required attribute. for an invalid control, set aria-invalid=\"true\" and point aria-describedby at the .s-error element so screen readers announce the message. see select for choosing from options and s-model for the binding details."},{"page":"Dropdown Menu","category":"Components","heading":"Dropdown Menu","slug":"comp-menu","anchor":"","snippet":"A dropdown menu hangs a short list of actions off a button. You click the button to reveal it, pick an action, and it gets out of the way, either because you chose something, clicked elsewhere, or pressed Escape. Like th","text":"dropdown menu a dropdown menu hangs a short list of actions off a button. you click the button to reveal it, pick an action, and it gets out of the way, either because you chose something, clicked elsewhere, or pressed escape. like the rest of the library it runs on one boolean in s-data, and the two dismissal paths, outside click and escape, are wired with plain event modifiers."},{"page":"Dropdown Menu","category":"Components","heading":"Toggling","slug":"comp-menu","anchor":"toggling","snippet":"The trigger runs @click=\"open = !open\", so clicking it flips the menu open and closed. The menu itself is a .s-menu shown with s-show, which keeps the markup in the DOM and toggles its visibility. The wrapper carries the","text":"toggling the trigger runs @click=\"open = !open\", so clicking it flips the menu open and closed. the menu itself is a .s-menu shown with s-show, which keeps the markup in the dom and toggles its visibility. the wrapper carries the class .s-anchor, which positions the menu absolutely just under the button, so the list floats over the page instead of pushing content down."},{"page":"Dropdown Menu","category":"Components","heading":"Closing again","slug":"comp-menu","anchor":"closing-again","snippet":"Two handlers cover the ways a menu should dismiss itself: - Outside click: @click.outside=\"open = false\" on the .s-menu fires when a click lands anywhere outside the menu, so clicking away closes it. - Escape: @keydown.e","text":"closing again two handlers cover the ways a menu should dismiss itself: - outside click: @click.outside=\"open = false\" on the .s-menu fires when a click lands anywhere outside the menu, so clicking away closes it. - escape: @keydown.escape=\"open = false\" on the wrapper closes the menu from the keyboard. because the whole thing shares one open value, the two paths cannot drift out of sync."},{"page":"Dropdown Menu","category":"Components","heading":"The items","slug":"comp-menu","anchor":"the-items","snippet":"The menu is a stack of building blocks you arrange to taste: - .s-menu-item is a single action. Use a <button for actions and an <a for links; both pick up the same styling. - .s-menu-label is a small uppercase heading y","text":"the items the menu is a stack of building blocks you arrange to taste: - .s-menu-item is a single action. use a <button for actions and an <a for links; both pick up the same styling. - .s-menu-label is a small uppercase heading you place above a group of related items to caption a section. - .s-menu-sep is a thin divider that splits the list into groups. - .is-danger on an item paints it in the danger color, which suits destructive actions like delete."},{"page":"Dropdown Menu","category":"Components","heading":"Accessibility","slug":"comp-menu","anchor":"accessibility","snippet":"The trigger is a real <button, so it is focusable and operable with Enter and Space out of the box, and each .s-menu-item is a button or link for the same reason. Set aria-haspopup=\"menu\" on the trigger and reflect the o","text":"accessibility the trigger is a real <button, so it is focusable and operable with enter and space out of the box, and each .s-menu-item is a button or link for the same reason. set aria-haspopup=\"menu\" on the trigger and reflect the open state with aria-expanded, bound to your state with s-bind, so assistive tech knows a menu is attached and whether it is showing. the escape handler gives keyboard users a quick way out, matching the behavior of native menus, and the outside-click handler keeps a stray open menu from lingering. when the menu opens, move focus to the first item so the arrow keys and typing land somewhere sensible. for a blocking confirmation rather than a list of actions, reach for the dialog; for free-form content in a floating panel, see the popover."},{"page":"Pagination","category":"Components","heading":"Pagination","slug":"comp-pagination","anchor":"","snippet":"Pagination splits a long list of results into numbered pages and gives people Prev and Next controls to move through them. One number of state, the current page, drives which button is active and when the ends disable. T","text":"pagination pagination splits a long list of results into numbered pages and gives people prev and next controls to move through them. one number of state, the current page, drives which button is active and when the ends disable. the number buttons themselves come straight out of a s-for."},{"page":"Pagination","category":"Components","heading":"A live pager","slug":"comp-pagination","anchor":"a-live-pager","snippet":"page holds the current page and total the count. Prev and Next clamp with Math.max and Math.min so they never step past the ends, and :disabled greys them out there. Each numbered button sets page to its own value and li","text":"a live pager page holds the current page and total the count. prev and next clamp with math.max and math.min so they never step past the ends, and :disabled greys them out there. each numbered button sets page to its own value and lights up through :class when it matches."},{"page":"Pagination","category":"Components","heading":"Notes","slug":"comp-pagination","anchor":"notes","snippet":"- Prev disables at page one and Next at the last page, driven by :disabled. The stylesheet fades a disabled control and turns off its pointer events, so it reads as unavailable and cannot be clicked. - Math.max(1, page -","text":"notes - prev disables at page one and next at the last page, driven by :disabled. the stylesheet fades a disabled control and turns off its pointer events, so it reads as unavailable and cannot be clicked. - math.max(1, page - 1) and math.min(total, page + 1) keep page inside range even if a control is clicked at the edge, so the state can never run off either end. - the is-active class fills the current page with your accent color. because it is bound with :class, exactly one number lights up at a time. - page is a plain number, so you can watch it to fetch the matching slice of data, or seed it from the url to deep-link a page. to show where the current page sits in your site hierarchy instead, see breadcrumb. the whole set is on the overview."},{"page":"Popover","category":"Components","heading":"Popover","slug":"comp-popover","anchor":"","snippet":"A popover is a small floating panel you open with a click to show rich content next to its trigger: a short form, a set of controls, a detail card. It is more than a Tooltip, which only shows a line of text on hover, and","text":"popover a popover is a small floating panel you open with a click to show rich content next to its trigger: a short form, a set of controls, a detail card. it is more than a tooltip, which only shows a line of text on hover, and looser than a dropdown menu, which holds a list of actions. the markup is a .s-anchor that positions the floating .s-popover, wired with the same handful of s- directives you already use."},{"page":"Popover","category":"Components","heading":"A live popover","slug":"comp-popover","anchor":"a-live-popover","snippet":"The .s-anchor establishes the positioning context, the button toggles open, and the .s-popover shows while open is true. Two directives handle dismissal: @click.outside closes the panel when you click anywhere else, and ","text":"a live popover the .s-anchor establishes the positioning context, the button toggles open, and the .s-popover shows while open is true. two directives handle dismissal: @click.outside closes the panel when you click anywhere else, and @keydown.escape closes it from the keyboard."},{"page":"Popover","category":"Components","heading":"A panel with controls","slug":"comp-popover","anchor":"a-panel-with-controls","snippet":"Because a popover holds real content, you can put inputs and buttons inside it. Clicks that land within the panel do not trigger @click.outside, so typing in a field keeps the popover open. Wire a confirming action to se","text":"a panel with controls because a popover holds real content, you can put inputs and buttons inside it. clicks that land within the panel do not trigger @click.outside, so typing in a field keeps the popover open. wire a confirming action to set open = false so the panel closes once the user is done."},{"page":"Popover","category":"Components","heading":"Positioning","slug":"comp-popover","anchor":"positioning","snippet":"The .s-popover is absolutely positioned against its .s-anchor, opening just below the trigger and aligned to its left edge. Because the anchor is the positioning context, keep the trigger and the panel together inside th","text":"positioning the .s-popover is absolutely positioned against its .s-anchor, opening just below the trigger and aligned to its left edge. because the anchor is the positioning context, keep the trigger and the panel together inside the same .s-anchor. to place the panel elsewhere, override top, left, or right on .s-popover in your own styles; to widen it past the default 260px, set a new width."},{"page":"Popover","category":"Components","heading":"Notes","slug":"comp-popover","anchor":"notes","snippet":"Toggling open drives everything, so a single boolean is the whole state model. Keep @click.outside and @keydown.escape on the panel and the anchor so the popover always has a way out, whether the user reaches for the mou","text":"notes toggling open drives everything, so a single boolean is the whole state model. keep @click.outside and @keydown.escape on the panel and the anchor so the popover always has a way out, whether the user reaches for the mouse or the keyboard. for accessibility, add aria-expanded bound to open on the trigger and point aria-controls at the panel's id, which you can generate with the $id magic. for a hover hint of plain text, use a tooltip. for a list of commands or links, reach for a dropdown menu, which shares the same .s-anchor and dismissal pattern. the event modifiers used here are covered on the events page."},{"page":"Progress & Spinner","category":"Components","heading":"Progress & Spinner","slug":"comp-progress","anchor":"","snippet":"Loading states tell people the app is still working. Summit gives you three primitives for them: a progress bar you drive from state, a spinner for waits whose length you cannot predict, and a skeleton that stands in for","text":"progress & spinner loading states tell people the app is still working. summit gives you three primitives for them: a progress bar you drive from state, a spinner for waits whose length you cannot predict, and a skeleton that stands in for content that has not arrived yet. the bar is the only interactive one, so it reads its width straight off a value in s-data."},{"page":"Progress & Spinner","category":"Components","heading":"Driving the width","slug":"comp-progress","anchor":"driving-the-width","snippet":"The bar is a .s-progress track with a single inner <span. You never set a value attribute; you set the span's width, and the CSS transition animates the fill. Bind it with :style so the width tracks whatever number lives","text":"driving the width the bar is a .s-progress track with a single inner <span. you never set a value attribute; you set the span's width, and the css transition animates the fill. bind it with :style so the width tracks whatever number lives in state. keep the number inside 0 to 100. clamping with math.max and math.min when you change it, as the demo above does, means a stray click can never push the fill past either end."},{"page":"Progress & Spinner","category":"Components","heading":"Spinner","slug":"comp-progress","anchor":"spinner","snippet":".s-spinner is a standalone element for indeterminate waits, when you know work is happening but not how far along it is. It needs no state and no inner markup, just the class. Pair it with a label so the reason for the w","text":"spinner .s-spinner is a standalone element for indeterminate waits, when you know work is happening but not how far along it is. it needs no state and no inner markup, just the class. pair it with a label so the reason for the wait is clear."},{"page":"Progress & Spinner","category":"Components","heading":"Skeleton","slug":"comp-progress","anchor":"skeleton","snippet":".s-skeleton is a shimmering placeholder block. Give it an inline width and height so it matches the shape of the content it is standing in for, then stack several to preview a whole card or list row.","text":"skeleton .s-skeleton is a shimmering placeholder block. give it an inline width and height so it matches the shape of the content it is standing in for, then stack several to preview a whole card or list row."},{"page":"Progress & Spinner","category":"Components","heading":"Notes","slug":"comp-progress","anchor":"notes","snippet":"- The bar carries no semantics on its own. Add role=\"progressbar\" with aria-valuemin, aria-valuemax, and a bound :aria-valuenow so screen readers announce the position, as the top demo does. - Give the spinner role=\"stat","text":"notes - the bar carries no semantics on its own. add role=\"progressbar\" with aria-valuemin, aria-valuemax, and a bound :aria-valuenow so screen readers announce the position, as the top demo does. - give the spinner role=\"status\" and an aria-label so its purpose reaches people who cannot see it turn. - both the spinner's spin and the skeleton's shimmer stop under prefers-reduced-motion, so you do not need to disable them yourself. - once a load finishes, a toast or an alert is a good way to confirm the result. see all the pieces on the ui library overview."},{"page":"Select","category":"Components","heading":"Select","slug":"comp-select","anchor":"","snippet":"Select lets you pick one value from a fixed list. Apply .s-select to a real <select element and bind it with s-model, so the chosen option flows straight into your state.","text":"select select lets you pick one value from a fixed list. apply .s-select to a real <select element and bind it with s-model, so the chosen option flows straight into your state."},{"page":"Select","category":"Components","heading":"Driving other state","slug":"comp-select","anchor":"driving-other-state","snippet":"Because the value is just a piece of your data, you can react to it anywhere. Here the choice reveals a note with s-show.","text":"driving other state because the value is just a piece of your data, you can react to it anywhere. here the choice reveals a note with s-show."},{"page":"Select","category":"Components","heading":"A real native select","slug":"comp-select","anchor":"a-real-native-select","snippet":".s-select styles the browser's own <select. It only restyles the closed control, so the open list stays the native menu. That is deliberate: you keep the platform's keyboard handling, type-ahead search, and the correct p","text":"a real native select .s-select styles the browser's own <select. it only restyles the closed control, so the open list stays the native menu. that is deliberate: you keep the platform's keyboard handling, type-ahead search, and the correct picker on touch devices for free, and there is no custom widget to maintain."},{"page":"Select","category":"Components","heading":"Accessibility","slug":"comp-select","anchor":"accessibility","snippet":"Since this is a real <select, screen readers, keyboard navigation, and mobile pickers all work with no extra wiring. Pair it with a <label by matching the label's for to the select's id, or use the $id magic when the id ","text":"accessibility since this is a real <select, screen readers, keyboard navigation, and mobile pickers all work with no extra wiring. pair it with a <label by matching the label's for to the select's id, or use the $id magic when the id is generated at runtime. for binding, coercion modifiers, and multiple selects, see forms and s-model; for free-text entry, see input."},{"page":"Switch","category":"Components","heading":"Switch","slug":"comp-switch","anchor":"","snippet":"A switch is an on/off control for a single setting, the kind you flip and it takes effect right away. Summit's switch is a styled <label class=\"s-switch\" wrapping a real checkbox, so binding it is exactly like binding a ","text":"switch a switch is an on/off control for a single setting, the kind you flip and it takes effect right away. summit's switch is a styled <label class=\"s-switch\" wrapping a real checkbox, so binding it is exactly like binding a checkbox: point s-model at a boolean and the track slides between states."},{"page":"Switch","category":"Components","heading":"A live switch","slug":"comp-switch","anchor":"a-live-switch","snippet":"The <input is visually hidden and holds the state; the .s-track is the sliding pill you see. Here the switch flips on, and s-show reveals a message only while it is true.","text":"a live switch the <input is visually hidden and holds the state; the .s-track is the sliding pill you see. here the switch flips on, and s-show reveals a message only while it is true."},{"page":"Switch","category":"Components","heading":"Disabled","slug":"comp-switch","anchor":"disabled","snippet":"Add the native disabled attribute to the input to lock a switch. It stops responding to clicks and keyboard input while keeping its current position, which is useful for a setting that depends on another choice.","text":"disabled add the native disabled attribute to the input to lock a switch. it stops responding to clicks and keyboard input while keeping its current position, which is useful for a setting that depends on another choice."},{"page":"Switch","category":"Components","heading":"Accessibility","slug":"comp-switch","anchor":"accessibility","snippet":"Under the hood a switch is a real <input type=\"checkbox\", so it is fully accessible with no extra work: it is reachable with Tab, toggles with Space, and announces its checked state to screen readers. Wrapping the contro","text":"accessibility under the hood a switch is a real <input type=\"checkbox\", so it is fully accessible with no extra work: it is reachable with tab, toggles with space, and announces its checked state to screen readers. wrapping the control in its <label makes the visible text the accessible name. if you want assistive tech to announce it as a switch rather than a checkbox, add role=\"switch\" to the input; the checked state maps to on and off automatically. for a set of choices rather than a single toggle, use checkbox & radio. see forms for the full picture of s-model bindings."},{"page":"Tabs","category":"Components","heading":"Tabs","slug":"comp-tabs","anchor":"","snippet":"Tabs let you split related content into panels and show one at a time. There is no widget to wire up: a tab is a real <button, the active one is marked with a class, and each panel is revealed with s-show. One piece of s","text":"tabs tabs let you split related content into panels and show one at a time. there is no widget to wire up: a tab is a real <button, the active one is marked with a class, and each panel is revealed with s-show. one piece of state, the name of the current tab, drives the whole thing."},{"page":"Tabs","category":"Components","heading":"A live tab bar","slug":"comp-tabs","anchor":"a-live-tab-bar","snippet":"The .s-tabs-list holds the buttons and draws the underline. Each .s-tab clicks to set tab to its own name, and :class paints is-active on whichever one matches. The panels below use s-show so only the current one renders","text":"a live tab bar the .s-tabs-list holds the buttons and draws the underline. each .s-tab clicks to set tab to its own name, and :class paints is-active on whichever one matches. the panels below use s-show so only the current one renders."},{"page":"Tabs","category":"Components","heading":"Soft variant","slug":"comp-tabs","anchor":"soft-variant","snippet":"Wrap the exact same markup in a parent with class=\"s-tabs-soft\" and the underlined bar becomes a padded pill group, with the active tab lifted onto a raised surface. Nothing about the behavior changes; only the container","text":"soft variant wrap the exact same markup in a parent with class=\"s-tabs-soft\" and the underlined bar becomes a padded pill group, with the active tab lifted onto a raised surface. nothing about the behavior changes; only the container class is added."},{"page":"Tabs","category":"Components","heading":"Notes","slug":"comp-tabs","anchor":"notes","snippet":"- The current tab is just a string on s-data, so you can set a default by starting it at the tab you want open, or restore it from a URL or storage. - Each tab is a real button, so it is reachable with Tab and activates ","text":"notes - the current tab is just a string on s-data, so you can set a default by starting it at the tab you want open, or restore it from a url or storage. - each tab is a real button, so it is reachable with tab and activates with enter or space for free. add role=\"tablist\" on the list and pair panels with role=\"tabpanel\" if you want the full aria tab semantics. - prefer s-show here so panel state stays in the dom as you switch. if a panel is expensive and should only mount when opened, use s-if instead. for stacked sections that expand in place rather than swap, see accordion. the whole set is on the overview."},{"page":"Toast","category":"Components","heading":"Toast","slug":"comp-toast","anchor":"","snippet":"A toast is a short, transient notification that slides into a corner to confirm an action or report a result, then clears itself after a moment. Toasts stack in a fixed .s-toast-viewport so several can show at once, newe","text":"toast a toast is a short, transient notification that slides into a corner to confirm an action or report a result, then clears itself after a moment. toasts stack in a fixed .s-toast-viewport so several can show at once, newest below the last. each one is a .s-toast you render from an array with s-for, which means adding and removing entries is all it takes to raise and clear them."},{"page":"Toast","category":"Components","heading":"A live toast","slug":"comp-toast","anchor":"a-live-toast","snippet":"The state is an array of toasts. The add() method pushes a new entry with a unique id from Date.now(), then schedules dismiss(id) with setTimeout so the toast clears itself after three seconds. Manual dismissal calls the","text":"a live toast the state is an array of toasts. the add() method pushes a new entry with a unique id from date.now(), then schedules dismiss(id) with settimeout so the toast clears itself after three seconds. manual dismissal calls the same dismiss(), which filters the entry out of the array."},{"page":"Toast","category":"Components","heading":"Auto-dismiss and manual dismiss","slug":"comp-toast","anchor":"auto-dismiss-and-manual-dismiss","snippet":"Every toast gets a setTimeout(() = this.dismiss(id), 3000) when it is added, so it disappears on its own after three seconds. The close button on each toast calls the same dismiss(id), letting the user clear it early. Be","text":"auto-dismiss and manual dismiss every toast gets a settimeout(() = this.dismiss(id), 3000) when it is added, so it disappears on its own after three seconds. the close button on each toast calls the same dismiss(id), letting the user clear it early. because both paths filter by the toast's id, dismissing one never disturbs the others still on screen. change the timeout to hold toasts longer, or drop it for a toast the user must close by hand."},{"page":"Toast","category":"Components","heading":"Success and danger variants","slug":"comp-toast","anchor":"success-and-danger-variants","snippet":"The left border of a toast carries its meaning. The base .s-toast uses the accent color; add .s-toast-success for a confirmation or .s-toast-danger for an error. Here add(type) picks the class and the copy from the kind ","text":"success and danger variants the left border of a toast carries its meaning. the base .s-toast uses the accent color; add .s-toast-success for a confirmation or .s-toast-danger for an error. here add(type) picks the class and the copy from the kind of toast you asked for."},{"page":"Toast","category":"Components","heading":"Notes","slug":"comp-toast","anchor":"notes","snippet":"The examples keep the toast list in local s-data so each is self-contained, but in a real app you want one viewport and a single list that any component can add to. Move the array and its add() and dismiss() methods into","text":"notes the examples keep the toast list in local s-data so each is self-contained, but in a real app you want one viewport and a single list that any component can add to. move the array and its add() and dismiss() methods into a global summit.store, render the viewport once near the root, and raise a toast from anywhere with $store.toasts.add(...). that way a save button in one corner and a form in another share the same stack instead of each managing its own. the .s-toast-viewport is position: fixed, so it floats above the page no matter where it sits in the markup. keep the :key=\"t.id\" on the s-for so summit tracks each toast by identity as the array changes, which keeps auto-dismiss and manual dismiss from removing the wrong one. for a persistent, inline message rather than a transient one, use an alert instead."},{"page":"Tooltip","category":"Components","heading":"Tooltip","slug":"comp-tooltip","anchor":"","snippet":"A tooltip is a small label that appears next to a control to explain what it does. Summit's version is pure CSS: it shows while the wrapper is hovered or holds focus, and hides again on its own. There is no s-data, no di","text":"tooltip a tooltip is a small label that appears next to a control to explain what it does. summit's version is pure css: it shows while the wrapper is hovered or holds focus, and hides again on its own. there is no s-data, no directive, and no javascript to wire up. you just nest the tip inside the trigger."},{"page":"Tooltip","category":"Components","heading":"Structure","slug":"comp-tooltip","anchor":"structure","snippet":"A tooltip is two parts inside one wrapper: - .s-tooltip is the wrapper. It positions the tip and listens for hover and focus through CSS alone. - Inside it goes your trigger (a button or link) and a <span class=\"s-tip\" h","text":"structure a tooltip is two parts inside one wrapper: - .s-tooltip is the wrapper. it positions the tip and listens for hover and focus through css alone. - inside it goes your trigger (a button or link) and a <span class=\"s-tip\" holding the text. the tip is hidden until the wrapper matches :hover or :focus-within, so it appears both when you point at the trigger and when a keyboard user tabs onto it. that is why the trigger should be a naturally focusable element. if you wrap something that is not focusable, add tabindex=\"0\" so keyboard users reach it too."},{"page":"Tooltip","category":"Components","heading":"Any trigger","slug":"comp-tooltip","anchor":"any-trigger","snippet":"The wrapper works around a button, an icon button, or plain inline text. Keep the tip short: it renders on a single line and does not wrap.","text":"any trigger the wrapper works around a button, an icon button, or plain inline text. keep the tip short: it renders on a single line and does not wrap."},{"page":"Tooltip","category":"Components","heading":"Notes","slug":"comp-tooltip","anchor":"notes","snippet":"- No state is involved. The tooltip needs no s-data and adds nothing to your JavaScript, so you can drop it anywhere, even into otherwise static markup. - Because it is CSS-only, the tip always sits above the trigger and","text":"notes - no state is involved. the tooltip needs no s-data and adds nothing to your javascript, so you can drop it anywhere, even into otherwise static markup. - because it is css-only, the tip always sits above the trigger and centered. it does not measure the viewport or flip to stay on screen, so avoid it right at the top edge of the page. - text stays on one line by design. for anything longer, or for content that should open on click and hold buttons or fields, reach for a popover instead. - browse the rest of the set on the ui library overview."},{"page":"UI Library","category":"Components","heading":"UI Library","slug":"components","anchor":"","snippet":"Summit ships a copy-in UI library: a set of accessible, token-themed components you drop straight into your HTML. There is no component runtime to install. Each one is plain markup plus a class, and the interactive ones ","text":"ui library summit ships a copy-in ui library: a set of accessible, token-themed components you drop straight into your html. there is no component runtime to install. each one is plain markup plus a class, and the interactive ones are wired with the same s- directives you already know. theme the whole set by changing a few css variables. it is built the way an ai agent likes to work: predictable class names, behavior that lives on the element, and nothing that needs a build step."},{"page":"UI Library","category":"Components","heading":"Using a component","slug":"components","anchor":"using-a-component","snippet":"Load the stylesheet once, then copy any component's markup. Or copy the rules you need out of components.css into your own styles. Every class is prefixed s-, so nothing collides with your existing CSS.","text":"using a component load the stylesheet once, then copy any component's markup. or copy the rules you need out of components.css into your own styles. every class is prefixed s-, so nothing collides with your existing css."},{"page":"UI Library","category":"Components","heading":"Theming","slug":"components","anchor":"theming","snippet":"Components read the same design tokens as the rest of Summit. Override them on :root (or any scope) and the whole set restyles. The most useful ones: Light and dark are handled for you through prefers-color-scheme and th","text":"theming components read the same design tokens as the rest of summit. override them on :root (or any scope) and the whole set restyles. the most useful ones: light and dark are handled for you through prefers-color-scheme and the data-theme attribute, exactly like this documentation."},{"page":"UI Library","category":"Components","heading":"Accessibility","slug":"components","anchor":"accessibility","snippet":"Interactive components ship with the roles and keyboard behavior you expect: focus rings on every control, Escape to close overlays, click-outside to dismiss menus, and labels wired to their inputs. Where a component nee","text":"accessibility interactive components ship with the roles and keyboard behavior you expect: focus rings on every control, escape to close overlays, click-outside to dismiss menus, and labels wired to their inputs. where a component needs an id to pair a label with a control, use the $id magic."},{"page":"UI Library","category":"Components","heading":"The components","slug":"components","anchor":"the-components","snippet":"Forms Button, Input, Select, Checkbox & Radio, Switch Data display Card, Badge & Tag, Alert, Avatar, Progress & Spinner Overlays Dialog, Dropdown Menu, Popover, Tooltip, Toast Navigation Tabs, Accordion, Breadcrumb, Pagi","text":"the components forms button, input, select, checkbox & radio, switch data display card, badge & tag, alert, avatar, progress & spinner overlays dialog, dropdown menu, popover, tooltip, toast navigation tabs, accordion, breadcrumb, pagination"},{"page":"Events","category":"Essentials","heading":"Events","slug":"events","anchor":"","snippet":"s-on listens for an event and runs an expression when it fires. The shorthand is @, which is what you will normally reach for.","text":"events s-on listens for an event and runs an expression when it fires. the shorthand is @, which is what you will normally reach for."},{"page":"Events","category":"Essentials","heading":"The event object","slug":"events","anchor":"the-event-object","snippet":"The current event is available as $event inside the expression. If the expression evaluates to a function, Summit calls it with the event, so a bare method reference works too:","text":"the event object the current event is available as $event inside the expression. if the expression evaluates to a function, summit calls it with the event, so a bare method reference works too:"},{"page":"Events","category":"Essentials","heading":"Modifiers","slug":"events","anchor":"modifiers","snippet":"Modifiers are dot-suffixed flags that adjust how the listener behaves. They stack in any order.","text":"modifiers modifiers are dot-suffixed flags that adjust how the listener behaves. they stack in any order."},{"page":"Events","category":"Essentials","heading":"Targets","slug":"events","anchor":"targets","snippet":"By default the listener is attached to the element. Redirect it with .window or .document, which is how you register global shortcuts.","text":"targets by default the listener is attached to the element. redirect it with .window or .document, which is how you register global shortcuts."},{"page":"Events","category":"Essentials","heading":"Keys","slug":"events","anchor":"keys","snippet":"Filter keyboard events to specific keys. Named aliases include .enter, .escape, .tab, .space, .up .down .left .right (also .arrow-up and friends), .page-up, .page-down, .home, .end, .delete, and .backspace. Any other key","text":"keys filter keyboard events to specific keys. named aliases include .enter, .escape, .tab, .space, .up .down .left .right (also .arrow-up and friends), .page-up, .page-down, .home, .end, .delete, and .backspace. any other key works by its name, for example .k. combine with system keys .shift, .ctrl, .alt, and .cmd (alias .meta) for shortcuts. this fires only on cmd/ctrl + k:"},{"page":"Events","category":"Essentials","heading":"Timing","slug":"events","anchor":"timing","snippet":".debounce waits until events stop before running; .throttle runs at most once per interval. Both default to 250ms and accept a custom duration.","text":"timing .debounce waits until events stop before running; .throttle runs at most once per interval. both default to 250ms and accept a custom duration."},{"page":"Events","category":"Essentials","heading":"Name transforms","slug":"events","anchor":"name-transforms","snippet":".camel turns a dashed event name into camelCase, and .dot restores dots in event names that used dashes: For dispatching your own events, see $dispatch. The full directive reference is on the s-on page.","text":"name transforms .camel turns a dashed event name into camelcase, and .dot restores dots in event names that used dashes: for dispatching your own events, see $dispatch. the full directive reference is on the s-on page."},{"page":"Forms","category":"Essentials","heading":"Forms","slug":"forms","anchor":"","snippet":"s-model creates a two-way binding between a form control and a piece of state. Type in the field and the state updates; change the state and the field follows.","text":"forms s-model creates a two-way binding between a form control and a piece of state. type in the field and the state updates; change the state and the field follows."},{"page":"Forms","category":"Essentials","heading":"Every control type","slug":"forms","anchor":"every-control-type","snippet":"s-model reads and writes the right property for each kind of input, so the same directive covers all of them. - Text, textarea, number, range: bound to the value. - Checkbox: a boolean when bound to a single control, or ","text":"every control type s-model reads and writes the right property for each kind of input, so the same directive covers all of them. - text, textarea, number, range: bound to the value. - checkbox: a boolean when bound to a single control, or an array of the checked values when several share one model. - radio: the value of the selected control. - select: the chosen value, or an array when the select is multiple."},{"page":"Forms","category":"Essentials","heading":"Building custom inputs","slug":"forms","anchor":"building-custom-inputs","snippet":"Every element with s-model exposes el.summitModel, an object with get and set, so a wrapper component can drive the same binding from custom controls. The full reference is on the s-model page.","text":"building custom inputs every element with s-model exposes el.summitmodel, an object with get and set, so a wrapper component can drive the same binding from custom controls. the full reference is on the s-model page."},{"page":"Summit.bind","category":"Globals & API","heading":"Summit.bind","slug":"globals-bind","anchor":"","snippet":"Summit.bind registers a named bundle of attributes, event handlers, and dynamic bindings so you can apply the whole set at once instead of repeating the same :class, @click, and aria- attributes on every element that nee","text":"summit.bind summit.bind registers a named bundle of attributes, event handlers, and dynamic bindings so you can apply the whole set at once instead of repeating the same :class, @click, and aria- attributes on every element that needs them. it is the reusable form of an object passed to s-bind."},{"page":"Summit.bind","category":"Globals & API","heading":"Registering a bundle","slug":"globals-bind","anchor":"registering-a-bundle","snippet":"Call Summit.bind(name, provider). The provider is a function that takes no arguments and returns an object of bindings: Summit.bind returns the Summit global, so it chains with your other registrations.","text":"registering a bundle call summit.bind(name, provider). the provider is a function that takes no arguments and returns an object of bindings: summit.bind returns the summit global, so it chains with your other registrations."},{"page":"Summit.bind","category":"Globals & API","heading":"What a bind provider returns","slug":"globals-bind","anchor":"what-a-bind-provider-returns","snippet":"The object uses the same shape s-bind accepts, and each kind of key is handled differently: - A plain key sets an attribute. Boolean attributes such as disabled are added or removed based on truthiness; a value of false ","text":"what a bind provider returns the object uses the same shape s-bind accepts, and each kind of key is handled differently: - a plain key sets an attribute. boolean attributes such as disabled are added or removed based on truthiness; a value of false or null removes the attribute. - an @event key adds an event listener. its value is the handler, called with the event. the listener is removed automatically when the element is torn down. - a :attr key is a dynamic binding. its value is a getter function that summit runs inside a reactive effect, so the attribute re-applies whenever the reactive state the getter reads changes. read that state from a store or other reactive source, since a bind provider has no component scope of its own."},{"page":"Summit.bind","category":"Globals & API","heading":"Applying a bundle","slug":"globals-bind","anchor":"applying-a-bundle","snippet":"Retrieve a registered provider with Summit.getBind(name), which returns the provider function (or undefined if none is registered). Calling it produces the bindings object, which you hand to s-bind. Add Summit to the exp","text":"applying a bundle retrieve a registered provider with summit.getbind(name), which returns the provider function (or undefined if none is registered). calling it produces the bindings object, which you hand to s-bind. add summit to the expression globals once so markup can reach it: because s-bind merges these bindings onto the element, you can still add element-specific attributes and directives alongside the shared bundle."},{"page":"Summit.data","category":"Globals & API","heading":"Summit.data","slug":"globals-data","anchor":"","snippet":"Summit.data registers a named component so you can reference it from any s-data attribute instead of repeating an inline object. It is the tool for anything beyond a one-off snippet: a counter, a dropdown, a form, a comp","text":"summit.data summit.data registers a named component so you can reference it from any s-data attribute instead of repeating an inline object. it is the tool for anything beyond a one-off snippet: a counter, a dropdown, a form, a component you use in ten places."},{"page":"Summit.data","category":"Globals & API","heading":"Registering a component","slug":"globals-data","anchor":"registering-a-component","snippet":"Call Summit.data(name, provider). The provider is a factory: a function that returns the state object. Returning the object from a function (rather than sharing one object) is what gives every use its own independent cop","text":"registering a component call summit.data(name, provider). the provider is a factory: a function that returns the state object. returning the object from a function (rather than sharing one object) is what gives every use its own independent copy. the returned object becomes reactive state exactly like an inline s-data object: properties are tracked, getters become cached derived values, and methods are bound so this is the component. see reactivity and state for how that state behaves."},{"page":"Summit.data","category":"Globals & API","heading":"Passing arguments","slug":"globals-data","anchor":"passing-arguments","snippet":"Reference the provider like a function call and Summit passes the arguments through. The argument list is evaluated in the parent scope, so you can hand in literals or values from an enclosing component. Bare names work ","text":"passing arguments reference the provider like a function call and summit passes the arguments through. the argument list is evaluated in the parent scope, so you can hand in literals or values from an enclosing component. bare names work too: s-data=\"counter\" calls the provider with no arguments, letting your parameter defaults (start = 0 above) take over."},{"page":"Summit.data","category":"Globals & API","heading":"A fresh copy every time","slug":"globals-data","anchor":"a-fresh-copy-every-time","snippet":"Because the provider is a function, each element that references it runs the factory again and gets a separate state object. Two s-data=\"counter\" blocks on the same page keep independent counts.","text":"a fresh copy every time because the provider is a function, each element that references it runs the factory again and gets a separate state object. two s-data=\"counter\" blocks on the same page keep independent counts."},{"page":"Summit.data","category":"Globals & API","heading":"Lifecycle hooks","slug":"globals-data","anchor":"lifecycle-hooks","snippet":"If the returned object has an init() method, Summit calls it once the component's state is set up. A destroy() method runs when the element is removed from the DOM. This works for named providers and inline objects alike","text":"lifecycle hooks if the returned object has an init() method, summit calls it once the component's state is set up. a destroy() method runs when the element is removed from the dom. this works for named providers and inline objects alike. see lifecycle for the full order of these hooks."},{"page":"Summit.data","category":"Globals & API","heading":"Chaining and timing","slug":"globals-data","anchor":"chaining-and-timing","snippet":"Summit.data returns the Summit global, so registrations chain: Registration is timing-safe. You can register a provider before or after Summit.start, and any matching s-data in the page, now or added later, resolves to i","text":"chaining and timing summit.data returns the summit global, so registrations chain: registration is timing-safe. you can register a provider before or after summit.start, and any matching s-data in the page, now or added later, resolves to it."},{"page":"Summit.directive","category":"Globals & API","heading":"Summit.directive","slug":"globals-directive","anchor":"","snippet":"Summit.directive adds a new s- attribute to Summit. Your directive runs when Summit initializes an element that carries it, with the same utilities the built-in directives use, so a directive you write is a first-class c","text":"summit.directive summit.directive adds a new s- attribute to summit. your directive runs when summit initializes an element that carries it, with the same utilities the built-in directives use, so a directive you write is a first-class citizen."},{"page":"Summit.directive","category":"Globals & API","heading":"Registering a directive","slug":"globals-directive","anchor":"registering-a-directive","snippet":"Call Summit.directive(name, handler, priority?). The name is used without the s- prefix. This registers s-tooltip: Summit.directive returns the Summit global, so registrations chain.","text":"registering a directive call summit.directive(name, handler, priority?). the name is used without the s- prefix. this registers s-tooltip: summit.directive returns the summit global, so registrations chain."},{"page":"Summit.directive","category":"Globals & API","heading":"The handler signature","slug":"globals-directive","anchor":"the-handler-signature","snippet":"The handler receives three arguments: (el, meta, utils). el is the element the directive is on. meta describes how the attribute was written: - name, the directive name without s- (here, \"tooltip\"). - value, the part aft","text":"the handler signature the handler receives three arguments: (el, meta, utils). el is the element the directive is on. meta describes how the attribute was written: - name, the directive name without s- (here, \"tooltip\"). - value, the part after a colon, or null if absent (\"click\" in s-on:click). - modifiers, the dot-separated modifiers as an array ([\"prevent\", \"stop\"]). - expression, the attribute's value string, ready to pass to evaluate. - raw, the original attribute name as written in the dom. utils is the directive author's toolkit: - evaluate(expression) runs a value expression in the element's scope. - evaluateaction(expression, locals?) runs statements (like an s-on handler), optionally with extra local variables. - effect(fn) creates a reactive effect that re-runs when its dependencies change and is torn down automatically with the element. - cleanup(fn) registers a teardown callback. - inittree(el, scopes?) and destroytree(el) initialize or tear down a subtree, used by structural directives that manage their own children. - scopes is the scope stack visible at this element. - makeenv(el, locals?) builds a fresh evaluation environment for an element. - summit is the global itself. use effect for anything that should react to state, and cleanup for anything that must be undone (a listener, a timer). errors thrown in a handler are caught and logged, so one broken directive will not stop the rest of the page."},{"page":"Summit.directive","category":"Globals & API","heading":"Priority","slug":"globals-directive","anchor":"priority","snippet":"The optional third argument sets ordering. When an element has several directives, lower numbers run earlier. The default is 100, so pass a smaller number to run before most directives and a larger one to run after:","text":"priority the optional third argument sets ordering. when an element has several directives, lower numbers run earlier. the default is 100, so pass a smaller number to run before most directives and a larger one to run after:"},{"page":"Summit.directive","category":"Globals & API","heading":"Timing","slug":"globals-directive","anchor":"timing","snippet":"Registration is timing-safe: register before or after Summit.start. Built-in directives are registered when Summit loads, and registering a custom directive with the same name overrides the built-in. For a fuller walkthr","text":"timing registration is timing-safe: register before or after summit.start. built-in directives are registered when summit loads, and registering a custom directive with the same name overrides the built-in. for a fuller walkthrough, see writing directives and plugins."},{"page":"Summit.magic","category":"Globals & API","heading":"Summit.magic","slug":"globals-magic","anchor":"","snippet":"Summit.magic adds a $-prefixed helper that every expression can reach, the same way the built-ins $el, $refs, and $store work. Use it to expose a value or a small function to your markup without threading it through comp","text":"summit.magic summit.magic adds a $-prefixed helper that every expression can reach, the same way the built-ins $el, $refs, and $store work. use it to expose a value or a small function to your markup without threading it through component state."},{"page":"Summit.magic","category":"Globals & API","heading":"Registering a magic","slug":"globals-magic","anchor":"registering-a-magic","snippet":"Call Summit.magic(name, factory). The name is used with a $ prefix in expressions. The factory receives a context object and returns whatever $name should be: Return a function and $name(...) calls it. Return a value and","text":"registering a magic call summit.magic(name, factory). the name is used with a $ prefix in expressions. the factory receives a context object and returns whatever $name should be: return a function and $name(...) calls it. return a value and $name is that value: summit.magic returns the summit global, so registrations chain."},{"page":"Summit.magic","category":"Globals & API","heading":"The magic context","slug":"globals-magic","anchor":"the-magic-context","snippet":"The factory is called with a context object for the element where the magic is used: - el is the element the expression is running on. - scopes is the scope stack visible at that element. - evaluate(expression) evaluates","text":"the magic context the factory is called with a context object for the element where the magic is used: - el is the element the expression is running on. - scopes is the scope stack visible at that element. - evaluate(expression) evaluates an expression in that scope. - effect(fn) creates a reactive effect torn down with the element. - cleanup(fn) registers a teardown callback. because the factory runs per use, a magic can be element-aware. this one reads the current element's id: use effect and cleanup when your magic sets up something reactive or disposable, so it is cleaned up when the element goes away."},{"page":"Summit.magic","category":"Globals & API","heading":"Timing","slug":"globals-magic","anchor":"timing","snippet":"Registration is timing-safe: register before or after Summit.start. Registering a magic with the name of a built-in overrides it. For a broader guide, see Writing directives and plugins.","text":"timing registration is timing-safe: register before or after summit.start. registering a magic with the name of a built-in overrides it. for a broader guide, see writing directives and plugins."},{"page":"Summit.plugin","category":"Globals & API","heading":"Summit.plugin","slug":"globals-plugin","anchor":"","snippet":"Summit.plugin installs a plugin: a function that registers a set of related features in one call. It is how you package data providers, directives, magics, stores, and binds together and share them across projects.","text":"summit.plugin summit.plugin installs a plugin: a function that registers a set of related features in one call. it is how you package data providers, directives, magics, stores, and binds together and share them across projects."},{"page":"Summit.plugin","category":"Globals & API","heading":"Installing a plugin","slug":"globals-plugin","anchor":"installing-a-plugin","snippet":"Call Summit.plugin(fn). Summit calls your function immediately with the Summit global, so the plugin registers everything it needs through the same API you would use by hand: A plugin is just a function, so it can regist","text":"installing a plugin call summit.plugin(fn). summit calls your function immediately with the summit global, so the plugin registers everything it needs through the same api you would use by hand: a plugin is just a function, so it can register as many features as it likes:"},{"page":"Summit.plugin","category":"Globals & API","heading":"Chaining","slug":"globals-plugin","anchor":"chaining","snippet":"Summit.plugin returns the Summit global, so you can install several plugins and keep registering: Everything a plugin registers is timing-safe, so it does not matter whether you install plugins before or after Summit.sta","text":"chaining summit.plugin returns the summit global, so you can install several plugins and keep registering: everything a plugin registers is timing-safe, so it does not matter whether you install plugins before or after summit.start. see writing directives and plugins for the pieces a plugin is built from."},{"page":"Summit.start","category":"Globals & API","heading":"Summit.start","slug":"globals-start","anchor":"","snippet":"Summit.start scans the DOM, brings every s- attribute to life, and begins watching for elements added later. The script-tag build calls it for you. The npm build leaves the timing to you, so you can register your data, d","text":"summit.start summit.start scans the dom, brings every s- attribute to life, and begins watching for elements added later. the script-tag build calls it for you. the npm build leaves the timing to you, so you can register your data, directives, and magics first."},{"page":"Summit.start","category":"Globals & API","heading":"Starting","slug":"globals-start","anchor":"starting","snippet":"Call Summit.start() to initialize the whole document, or pass a root element to initialize just that subtree: When no root is given, Summit starts from document.body. After the initial pass it keeps a watcher on the docu","text":"starting call summit.start() to initialize the whole document, or pass a root element to initialize just that subtree: when no root is given, summit starts from document.body. after the initial pass it keeps a watcher on the document, so any s- markup you add later comes alive on its own and torn-down elements are cleaned up."},{"page":"Summit.start","category":"Globals & API","heading":"It runs once","slug":"globals-start","anchor":"it-runs-once","snippet":"Summit.start is idempotent. The first call initializes the DOM; later calls return immediately and do nothing. That makes it safe to call after registering features without worrying about double-initialization: The scrip","text":"it runs once summit.start is idempotent. the first call initializes the dom; later calls return immediately and do nothing. that makes it safe to call after registering features without worrying about double-initialization: the script-tag build already calls start() once the dom is ready, so calling it again yourself is harmless."},{"page":"Summit.start","category":"Globals & API","heading":"Lifecycle events","slug":"globals-start","anchor":"lifecycle-events","snippet":"start dispatches two events on document, so you can hook into initialization: - summit:init fires just before Summit walks the DOM. - summit:initialized fires after the initial pass completes.","text":"lifecycle events start dispatches two events on document, so you can hook into initialization: - summit:init fires just before summit walks the dom. - summit:initialized fires after the initial pass completes."},{"page":"Summit.start","category":"Globals & API","heading":"Checking status and version","slug":"globals-start","anchor":"checking-status-and-version","snippet":"Summit.started() returns true once start has run, and false before: Summit.version is the version string of the running build: For install options and manual-start setup, see Installation.","text":"checking status and version summit.started() returns true once start has run, and false before: summit.version is the version string of the running build: for install options and manual-start setup, see installation."},{"page":"Summit.store","category":"Globals & API","heading":"Summit.store","slug":"globals-store","anchor":"","snippet":"State that many components share lives in a store, not in s-data. Summit.store both creates a store and reads one back, depending on whether you pass a value. Every store is reactive, so any expression that reads it upda","text":"summit.store state that many components share lives in a store, not in s-data. summit.store both creates a store and reads one back, depending on whether you pass a value. every store is reactive, so any expression that reads it updates when it changes."},{"page":"Summit.store","category":"Globals & API","heading":"Creating a store","slug":"globals-store","anchor":"creating-a-store","snippet":"Call Summit.store(name, value) with a starting value. Pass an object for structured state: An object store is made reactive the same way a component is: getters become cached derived values and methods are bound so this ","text":"creating a store call summit.store(name, value) with a starting value. pass an object for structured state: an object store is made reactive the same way a component is: getters become cached derived values and methods are bound so this is the store. if the object has an init() method, summit calls it once, right after the store is created. primitives work too, and stay reactive because every store lives under one reactive root:"},{"page":"Summit.store","category":"Globals & API","heading":"Reading a store","slug":"globals-store","anchor":"reading-a-store","snippet":"Call Summit.store(name) with no value to read the current one: In markup, reach any store through the $store magic. Reads track, so the view stays in sync, and writes are reactive out of the box:","text":"reading a store call summit.store(name) with no value to read the current one: in markup, reach any store through the $store magic. reads track, so the view stays in sync, and writes are reactive out of the box:"},{"page":"Summit.store","category":"Globals & API","heading":"Return values","slug":"globals-store","anchor":"return-values","snippet":"The call returns something useful in every form: - Summit.store(\"cart\", { ... }) returns the reactive store object. - Summit.store(\"darkMode\", false) returns the stored value. - Summit.store(\"cart\") returns the store's c","text":"return values the call returns something useful in every form: - summit.store(\"cart\", { ... }) returns the reactive store object. - summit.store(\"darkmode\", false) returns the stored value. - summit.store(\"cart\") returns the store's current value."},{"page":"Summit.store","category":"Globals & API","heading":"Updating a store","slug":"globals-store","anchor":"updating-a-store","snippet":"Mutate an object store through its own methods or properties, from JavaScript or from an expression, and every reader updates: For a primitive store, assign through $store in an expression ($store.darkMode = true), or ca","text":"updating a store mutate an object store through its own methods or properties, from javascript or from an expression, and every reader updates: for a primitive store, assign through $store in an expression ($store.darkmode = true), or call summit.store(name, value) again from javascript to replace the value. to create the store, see the object and primitive examples above."},{"page":"Installation","category":"Essentials","heading":"Installation","slug":"installation","anchor":"","snippet":"Summit ships two ways: a zero-build script you drop into any page, and an npm package for projects that use a bundler. Both give you the same runtime.","text":"installation summit ships two ways: a zero-build script you drop into any page, and an npm package for projects that use a bundler. both give you the same runtime."},{"page":"Installation","category":"Essentials","heading":"Script tag","slug":"installation","anchor":"script-tag","snippet":"The fastest path. Add one tag to your <head and Summit starts on its own once the DOM is ready. Use defer so it runs after the document is parsed. That is the entire setup. Any s- attributes already in the page come aliv","text":"script tag the fastest path. add one tag to your <head and summit starts on its own once the dom is ready. use defer so it runs after the document is parsed. that is the entire setup. any s- attributes already in the page come alive, and so does anything you add later, because summit watches the dom for changes. pin a version in production, for example summitjs@0.1.0, so a new release can never change your site without you knowing."},{"page":"Installation","category":"Essentials","heading":"npm","slug":"installation","anchor":"npm","snippet":"For projects with a build step, install the package: Import the Summit global, register anything you need, then start it yourself. Nothing runs until you call start(). The package ships ES modules, a CommonJS build, and ","text":"npm for projects with a build step, install the package: import the summit global, register anything you need, then start it yourself. nothing runs until you call start(). the package ships es modules, a commonjs build, and typescript types, so editor autocomplete and type checking work out of the box."},{"page":"Installation","category":"Essentials","heading":"Content-Security-Policy","slug":"installation","anchor":"content-security-policy","snippet":"Summit never calls eval or new Function. Expressions in your markup are parsed and interpreted by Summit itself, so a strict policy needs no 'unsafe-eval': Everything on this documentation site runs under exactly that co","text":"content-security-policy summit never calls eval or new function. expressions in your markup are parsed and interpreted by summit itself, so a strict policy needs no 'unsafe-eval': everything on this documentation site runs under exactly that constraint."},{"page":"Installation","category":"Essentials","heading":"Starting manually","slug":"installation","anchor":"starting-manually","snippet":"The script-tag build starts automatically. If you would rather control the moment yourself, for example to register custom directives first, call Summit.start() and it will only run once: See Summit.start for the details","text":"starting manually the script-tag build starts automatically. if you would rather control the moment yourself, for example to register custom directives first, call summit.start() and it will only run once: see summit.start for the details."},{"page":"Lifecycle","category":"Essentials","heading":"Lifecycle","slug":"lifecycle","anchor":"","snippet":"Summit manages a component from the moment its element appears to the moment it leaves. You can hook into that lifecycle in three places.","text":"lifecycle summit manages a component from the moment its element appears to the moment it leaves. you can hook into that lifecycle in three places."},{"page":"Lifecycle","category":"Essentials","heading":"Initialization","slug":"lifecycle","anchor":"initialization","snippet":"When a component is set up, Summit calls its init() method if it has one. This is the place for setup work: reading storage, starting a timer, fetching data. For a one-off expression rather than a method, use the s-init ","text":"initialization when a component is set up, summit calls its init() method if it has one. this is the place for setup work: reading storage, starting a timer, fetching data. for a one-off expression rather than a method, use the s-init directive:"},{"page":"Lifecycle","category":"Essentials","heading":"Reacting to changes","slug":"lifecycle","anchor":"reacting-to-changes","snippet":"Most of the time you do not watch for changes yourself: any expression that reads state re-runs when that state changes. For an explicit side effect that should re-run on change, use s-effect: To watch a single value and","text":"reacting to changes most of the time you do not watch for changes yourself: any expression that reads state re-runs when that state changes. for an explicit side effect that should re-run on change, use s-effect: to watch a single value and get both the new and old value, use the $watch magic."},{"page":"Lifecycle","category":"Essentials","heading":"Automatic setup for new nodes","slug":"lifecycle","anchor":"automatic-setup-for-new-nodes","snippet":"Summit watches the document with a MutationObserver. Any element you add later, whether by hand, from a fetch, or from another library, is initialized the moment it enters the DOM. There is nothing to call and no load or","text":"automatic setup for new nodes summit watches the document with a mutationobserver. any element you add later, whether by hand, from a fetch, or from another library, is initialized the moment it enters the dom. there is nothing to call and no load order to get right."},{"page":"Lifecycle","category":"Essentials","heading":"Cleanup","slug":"lifecycle","anchor":"cleanup","snippet":"When an element is removed, Summit tears its component down and runs every cleanup registered for it: event listeners are removed, effects are stopped, and timers you registered through a directive are cleared. Keyed s-f","text":"cleanup when an element is removed, summit tears its component down and runs every cleanup registered for it: event listeners are removed, effects are stopped, and timers you registered through a directive are cleared. keyed s-for reuses nodes across updates, so state is preserved for rows that merely moved."},{"page":"Lifecycle","category":"Essentials","heading":"Waiting for the DOM","slug":"lifecycle","anchor":"waiting-for-the-dom","snippet":"After you change state, the DOM updates on the next tick. To run code after that update lands, use $nextTick:","text":"waiting for the dom after you change state, the dom updates on the next tick. to run code after that update lands, use $nexttick:"},{"page":"Lifecycle","category":"Essentials","heading":"Page-level events","slug":"lifecycle","anchor":"page-level-events","snippet":"Summit dispatches summit:init on document just before it initializes the page, and summit:initialized once it has finished. Listen for them to run code around startup, for example to register a plugin before anything ren","text":"page-level events summit dispatches summit:init on document just before it initializes the page, and summit:initialized once it has finished. listen for them to run code around startup, for example to register a plugin before anything renders."},{"page":"$data","category":"Magic Properties","heading":"$data","slug":"magic-data","anchor":"","snippet":"$data is the current component's reactive state object, the same object you declared with s-data. Most of the time you read state by name, but when you need the whole object at once, $data hands it to you. Reads and writ","text":"$data $data is the current component's reactive state object, the same object you declared with s-data. most of the time you read state by name, but when you need the whole object at once, $data hands it to you. reads and writes through it stay reactive. writing $data.name updates the same state as writing name, so the text above re-renders. $data is just the state object itself, reached as a value."},{"page":"$data","category":"Magic Properties","heading":"When to use it","slug":"magic-data","anchor":"when-to-use-it","snippet":"Reach for $data when you want to work with the state as an object rather than by individual names, for example to serialize it, iterate its keys, pass it to a helper, or reset it in one assignment.","text":"when to use it reach for $data when you want to work with the state as an object rather than by individual names, for example to serialize it, iterate its keys, pass it to a helper, or reset it in one assignment."},{"page":"$data","category":"Magic Properties","heading":"Which component","slug":"magic-data","anchor":"which-component","snippet":"$data is the state of the nearest component. In nested components the inner s-data wins, so $data inside a child gives the child's state, not the parent's. To reach shared state across components, use $store instead.","text":"which component $data is the state of the nearest component. in nested components the inner s-data wins, so $data inside a child gives the child's state, not the parent's. to reach shared state across components, use $store instead."},{"page":"$dispatch","category":"Magic Properties","heading":"$dispatch","slug":"magic-dispatch","anchor":"","snippet":"$dispatch creates and dispatches a CustomEvent from the current element, so components can talk to each other through the DOM. The signature is $dispatch(name, detail?, options?). The button dispatches notify, the event ","text":"$dispatch $dispatch creates and dispatches a customevent from the current element, so components can talk to each other through the dom. the signature is $dispatch(name, detail?, options?). the button dispatches notify, the event bubbles up to the surrounding component, and its @notify handler reads the payload from $event.detail."},{"page":"$dispatch","category":"Magic Properties","heading":"The payload","slug":"magic-dispatch","anchor":"the-payload","snippet":"Whatever you pass as the second argument becomes event.detail. Read it in a listener as $event.detail.","text":"the payload whatever you pass as the second argument becomes event.detail. read it in a listener as $event.detail."},{"page":"$dispatch","category":"Magic Properties","heading":"Defaults and options","slug":"magic-dispatch","anchor":"defaults-and-options","snippet":"The event is created with bubbles, composed, and cancelable all set to true, so it rises through ancestors, crosses shadow DOM boundaries, and can be cancelled. Pass a third argument to override any of those. $dispatch r","text":"defaults and options the event is created with bubbles, composed, and cancelable all set to true, so it rises through ancestors, crosses shadow dom boundaries, and can be cancelled. pass a third argument to override any of those. $dispatch returns false when a listener calls preventdefault() on the event, and true otherwise."},{"page":"$dispatch","category":"Magic Properties","heading":"Listening from elsewhere","slug":"magic-dispatch","anchor":"listening-from-elsewhere","snippet":"Because the event bubbles, any ancestor can listen with @event-name. To catch an event no matter where it was dispatched, listen on the window with the .window modifier. For the full listener reference, including modifie","text":"listening from elsewhere because the event bubbles, any ancestor can listen with @event-name. to catch an event no matter where it was dispatched, listen on the window with the .window modifier. for the full listener reference, including modifiers, see the s-on and events pages."},{"page":"$el","category":"Magic Properties","heading":"$el","slug":"magic-el","anchor":"","snippet":"$el is the DOM element the current expression runs on. It is the element that carries the directive, not the component root, so inside @click it is the element you clicked and inside s-text it is the element being filled","text":"$el $el is the dom element the current expression runs on. it is the element that carries the directive, not the component root, so inside @click it is the element you clicked and inside s-text it is the element being filled. each expression sees its own $el. in the demo above $el is the button, so writing to $el.textcontent changes only the button."},{"page":"$el","category":"Magic Properties","heading":"Reading from the element","slug":"magic-el","anchor":"reading-from-the-element","snippet":"Because $el is a real element, the whole DOM API is available. This is useful for reading a property straight off the node instead of mirroring it in state.","text":"reading from the element because $el is a real element, the whole dom api is available. this is useful for reading a property straight off the node instead of mirroring it in state."},{"page":"$el","category":"Magic Properties","heading":"Reaching the component root","slug":"magic-el","anchor":"reaching-the-component-root","snippet":"$el is always the nearest element, which is often deep inside a component. When you want the element that owns the s-data instead, use $root. To reach other named elements in the component, use $refs.","text":"reaching the component root $el is always the nearest element, which is often deep inside a component. when you want the element that owns the s-data instead, use $root. to reach other named elements in the component, use $refs."},{"page":"$id","category":"Magic Properties","heading":"$id","slug":"magic-id","anchor":"","snippet":"$id generates a stable, unique id, which is exactly what you need to pair a label with its input or to wire up aria- attributes without hand-writing ids that might collide. The signature is $id(name, key?). Both calls to","text":"$id $id generates a stable, unique id, which is exactly what you need to pair a label with its input or to wire up aria- attributes without hand-writing ids that might collide. the signature is $id(name, key?). both calls to $id('email') return the same value, so the label's for matches the input's id. the generated id is unique on the page, so you can render this component many times and every copy stays correctly paired."},{"page":"$id","category":"Magic Properties","heading":"Scoping ids with s-id","slug":"magic-id","anchor":"scoping-ids-with-s-id","snippet":"By default each $id call produces a fresh page-unique id, which means two separate calls would not agree. The s-id directive fixes that: it opens an id group on an element, and every $id('name') inside that subtree retur","text":"scoping ids with s-id by default each $id call produces a fresh page-unique id, which means two separate calls would not agree. the s-id directive fixes that: it opens an id group on an element, and every $id('name') inside that subtree returns the same id for that name. pass s-id an array of the names you want to scope. groups nest: an inner s-id shadows an outer one for the names it lists."},{"page":"$id","category":"Magic Properties","heading":"Ids in a list","slug":"magic-id","anchor":"ids-in-a-list","snippet":"Inside an s-for loop, open a fresh group per row so each row gets its own ids and none of them collide.","text":"ids in a list inside an s-for loop, open a fresh group per row so each row gets its own ids and none of them collide."},{"page":"$id","category":"Magic Properties","heading":"The key argument","slug":"magic-id","anchor":"the-key-argument","snippet":"The optional second argument appends a key to the id, which is handy when one group needs a distinct id per item without a wrapper element. Every $id('opt', choice) for a given choice returns the same id, so labels and r","text":"the key argument the optional second argument appends a key to the id, which is handy when one group needs a distinct id per item without a wrapper element. every $id('opt', choice) for a given choice returns the same id, so labels and radios line up per option."},{"page":"$nextTick","category":"Magic Properties","heading":"$nextTick","slug":"magic-nextTick","anchor":"","snippet":"When you change state, Summit updates the DOM on the next microtask, not immediately. $nextTick lets you run code after that update has landed, so you can read the freshly rendered DOM. It takes an optional callback and ","text":"$nexttick when you change state, summit updates the dom on the next microtask, not immediately. $nexttick lets you run code after that update has landed, so you can read the freshly rendered dom. it takes an optional callback and returns a promise. reading the element right after changing msg still sees the old text, because the dom has not updated yet. reading inside $nexttick sees the new text."},{"page":"$nextTick","category":"Magic Properties","heading":"Awaiting the update","slug":"magic-nextTick","anchor":"awaiting-the-update","snippet":"$nextTick returns a promise, so in an async method you can await it instead of passing a callback. A common use is focusing an element that only just appeared, which is why you will often see $nextTick paired with $refs.","text":"awaiting the update $nexttick returns a promise, so in an async method you can await it instead of passing a callback. a common use is focusing an element that only just appeared, which is why you will often see $nexttick paired with $refs."},{"page":"$refs","category":"Magic Properties","heading":"$refs","slug":"magic-refs","anchor":"","snippet":"$refs is an object of the elements in the current component that you tagged with s-ref, keyed by their ref name. It is how you reach a specific element to focus it, measure it, or call a method on it, without a query sel","text":"$refs $refs is an object of the elements in the current component that you tagged with s-ref, keyed by their ref name. it is how you reach a specific element to focus it, measure it, or call a method on it, without a query selector. mark an element with s-ref=\"name\" and it appears as $refs.name."},{"page":"$refs","category":"Magic Properties","heading":"Naming a ref","slug":"magic-refs","anchor":"naming-a-ref","snippet":"By default the literal attribute value is the name, so s-ref=\"field\" registers under field. When the value resolves to a non-empty string it is treated as a dynamic name instead, which is what you want inside a loop wher","text":"naming a ref by default the literal attribute value is the name, so s-ref=\"field\" registers under field. when the value resolves to a non-empty string it is treated as a dynamic name instead, which is what you want inside a loop where each element needs its own key. the full rules are on the s-ref page."},{"page":"$refs","category":"Magic Properties","heading":"Scope and lifetime","slug":"magic-refs","anchor":"scope-and-lifetime","snippet":"$refs collects the refs for the nearest component, the closest s-data ancestor. A ref registered inside a child component is not visible from a parent, and vice versa. When an element leaves the DOM its entry is removed,","text":"scope and lifetime $refs collects the refs for the nearest component, the closest s-data ancestor. a ref registered inside a child component is not visible from a parent, and vice versa. when an element leaves the dom its entry is removed, so $refs only ever holds elements that are currently mounted. refs are populated as elements initialize, so read them from an event handler or inside $nexttick rather than at the top of init(), when a later sibling may not exist yet."},{"page":"$root","category":"Magic Properties","heading":"$root","slug":"magic-root","anchor":"","snippet":"$root is the root element of the nearest component, the closest ancestor (or the element itself) that carries an s-data. Wherever you are in the markup, $root points back at the element that owns the component. Even thou","text":"$root $root is the root element of the nearest component, the closest ancestor (or the element itself) that carries an s-data. wherever you are in the markup, $root points back at the element that owns the component. even though the button is nested two levels deep, $root reaches past its wrappers to the outer s-data element."},{"page":"$root","category":"Magic Properties","heading":"$root versus $el","slug":"magic-root","anchor":"root-versus-el","snippet":"$el is the element the current expression runs on, which changes from one directive to the next. $root is fixed for the whole component: it is the same element for every expression inside it. Use $el for the element at h","text":"$root versus $el $el is the element the current expression runs on, which changes from one directive to the next. $root is fixed for the whole component: it is the same element for every expression inside it. use $el for the element at hand and $root when you need the component's outer boundary, for example to scope a query or read a data attribute set on the root. if the expression is outside any component, $root is null."},{"page":"$store","category":"Magic Properties","heading":"$store","slug":"magic-store","anchor":"","snippet":"$store is the reactive root that holds every store you register with Summit.store. Reading a store value inside an expression tracks it, and writing to one triggers an update, so a change made in one component reaches ev","text":"$store $store is the reactive root that holds every store you register with summit.store. reading a store value inside an expression tracks it, and writing to one triggers an update, so a change made in one component reaches every component that reads it. register a store by name, then reach it as $store.<name from any expression."},{"page":"$store","category":"Magic Properties","heading":"Single values","slug":"magic-store","anchor":"single-values","snippet":"A store does not have to be an object. Single values are reactive too, so a plain boolean works and its writes still update every reader. Because reads track and writes trigger, $store.darkMode = !$store.darkMode is reac","text":"single values a store does not have to be an object. single values are reactive too, so a plain boolean works and its writes still update every reader. because reads track and writes trigger, $store.darkmode = !$store.darkmode is reactive with nothing extra to wire up."},{"page":"$store","category":"Magic Properties","heading":"When to reach for it","slug":"magic-store","anchor":"when-to-reach-for-it","snippet":"Use $store for state that many, otherwise unrelated components need to share. For state that belongs to one component and its children, keep it in s-data. See Summit.store for creating and reading stores from JavaScript.","text":"when to reach for it use $store for state that many, otherwise unrelated components need to share. for state that belongs to one component and its children, keep it in s-data. see summit.store for creating and reading stores from javascript."},{"page":"$watch","category":"Magic Properties","heading":"$watch","slug":"magic-watch","anchor":"","snippet":"$watch runs a callback whenever a watched expression changes. The signature is $watch(expression, callback), where expression is a string evaluated in the component's scope and callback receives the new value and the old","text":"$watch $watch runs a callback whenever a watched expression changes. the signature is $watch(expression, callback), where expression is a string evaluated in the component's scope and callback receives the new value and the old value. set the watcher up once, in s-init or in a component's init() method. the callback then fires on every later change to the expression."},{"page":"$watch","category":"Magic Properties","heading":"It only fires on a real change","slug":"magic-watch","anchor":"it-only-fires-on-a-real-change","snippet":"$watch compares values and only calls your callback when the value actually changes. That means you can mutate the watched value from inside the callback without spinning into an infinite loop, which is a common trap in ","text":"it only fires on a real change $watch compares values and only calls your callback when the value actually changes. that means you can mutate the watched value from inside the callback without spinning into an infinite loop, which is a common trap in other frameworks."},{"page":"$watch","category":"Magic Properties","heading":"It returns an unwatch function","slug":"magic-watch","anchor":"it-returns-an-unwatch-function","snippet":"$watch returns a function that stops the watcher. Call it when you no longer need the callback. After you stop the watcher the callback no longer runs, even as n keeps changing. You rarely need to call this by hand, thou","text":"it returns an unwatch function $watch returns a function that stops the watcher. call it when you no longer need the callback. after you stop the watcher the callback no longer runs, even as n keeps changing. you rarely need to call this by hand, though: a watcher is torn down automatically when its component leaves the dom."},{"page":"$watch","category":"Magic Properties","heading":"Watching derived values","slug":"magic-watch","anchor":"watching-derived-values","snippet":"The first argument is any expression, not just a property name, so you can watch a nested path or a computed result. For a side effect that should re-run without needing the old value, reach for s-effect instead.","text":"watching derived values the first argument is any expression, not just a property name, so you can watch a nested path or a computed result. for a side effect that should re-run without needing the old value, reach for s-effect instead."},{"page":"batch()","category":"Reactivity","heading":"batch()","slug":"reactive-batch","anchor":"","snippet":"Without batching, each write to a signal or reactive property triggers its dependent effects right away. When you make several related writes, that can run the same effect more than once. batch() defers those triggers un","text":"batch() without batching, each write to a signal or reactive property triggers its dependent effects right away. when you make several related writes, that can run the same effect more than once. batch() defers those triggers until the whole function has run, then flushes each affected effect a single time."},{"page":"batch()","category":"Reactivity","heading":"Grouping writes","slug":"reactive-batch","anchor":"grouping-writes","snippet":"Outside a batch, the two writes would run the effect twice. Inside one, the effect is queued once and runs after the batch closes. Duplicate triggers of the same effect are collapsed.","text":"grouping writes outside a batch, the two writes would run the effect twice. inside one, the effect is queued once and runs after the batch closes. duplicate triggers of the same effect are collapsed."},{"page":"batch()","category":"Reactivity","heading":"Return value","slug":"reactive-batch","anchor":"return-value","snippet":"batch() returns whatever its function returns, so you can compute and commit in one step.","text":"return value batch() returns whatever its function returns, so you can compute and commit in one step."},{"page":"batch()","category":"Reactivity","heading":"Nesting","slug":"reactive-batch","anchor":"nesting","snippet":"Batches nest. Only the outermost batch flushes effects, so wrapping already-batched code adds no extra runs.","text":"nesting batches nest. only the outermost batch flushes effects, so wrapping already-batched code adds no extra runs."},{"page":"batch()","category":"Reactivity","heading":"Batch versus the scheduler","slug":"reactive-batch","anchor":"batch-versus-the-scheduler","snippet":"batch() coalesces synchronous writes within one function. It is different from the microtask scheduling that DOM-facing directives use, where updates are already deferred to the next tick. When you need to read the DOM a","text":"batch versus the scheduler batch() coalesces synchronous writes within one function. it is different from the microtask scheduling that dom-facing directives use, where updates are already deferred to the next tick. when you need to read the dom after updates apply, await nexttick() instead. batch is also available on the global as summit.batch. for the trigger and flush mechanism underneath, see how reactivity works."},{"page":"computed()","category":"Reactivity","heading":"computed()","slug":"reactive-computed","anchor":"","snippet":"A computed value is derived from other reactive state and cached. It runs its getter once, remembers the result, and only recomputes when one of the values it read has changed. That makes it cheaper than a plain function","text":"computed() a computed value is derived from other reactive state and cached. it runs its getter once, remembers the result, and only recomputes when one of the values it read has changed. that makes it cheaper than a plain function you call repeatedly, which would redo the work on every call."},{"page":"computed()","category":"Reactivity","heading":"Creating a computed","slug":"reactive-computed","anchor":"creating-a-computed","snippet":"Pass computed a getter function. It returns a computed you read the same way as a signal, by calling it. Like a signal, a computed is a function, not an object with a .value. Read it by calling it.","text":"creating a computed pass computed a getter function. it returns a computed you read the same way as a signal, by calling it. like a signal, a computed is a function, not an object with a .value. read it by calling it."},{"page":"computed()","category":"Reactivity","heading":"Caching and laziness","slug":"reactive-computed","anchor":"caching-and-laziness","snippet":"A computed is lazy: the getter does not run until something reads the computed for the first time. After that, the cached result is returned on every read until a dependency changes. Reading a computed twice in a row doe","text":"caching and laziness a computed is lazy: the getter does not run until something reads the computed for the first time. after that, the cached result is returned on every read until a dependency changes. reading a computed twice in a row does the work once. only a change to a dependency such as price invalidates the cache."},{"page":"computed()","category":"Reactivity","heading":"Composition","slug":"reactive-computed","anchor":"composition","snippet":"Because a computed both reads dependencies and is itself readable, computeds chain. Reading one computed inside another subscribes to it, and reading a computed inside an effect() re-runs the effect when the computed's r","text":"composition because a computed both reads dependencies and is itself readable, computeds chain. reading one computed inside another subscribes to it, and reading a computed inside an effect() re-runs the effect when the computed's result changes."},{"page":"computed()","category":"Reactivity","heading":"Peeking","slug":"reactive-computed","anchor":"peeking","snippet":".peek() returns the current result, recomputing first if it is stale, without subscribing the active effect.","text":"peeking .peek() returns the current result, recomputing first if it is stale, without subscribing the active effect."},{"page":"computed()","category":"Reactivity","heading":"Where computeds show up","slug":"reactive-computed","anchor":"where-computeds-show-up","snippet":"The getters you define in s-data are cached the same way a computed is, which is why a derived value like a full name recomputes only when its parts change. See Derived values for the HTML-first form, and How reactivity ","text":"where computeds show up the getters you define in s-data are cached the same way a computed is, which is why a derived value like a full name recomputes only when its parts change. see derived values for the html-first form, and how reactivity works for the tracking model underneath. computed is also available on the global as summit.computed."},{"page":"effect()","category":"Reactivity","heading":"effect()","slug":"reactive-effect","anchor":"","snippet":"An effect runs a function and re-runs it whenever any reactive value it read during its last run changes. This is the heart of Summit: every reactive directive is a kind of effect. Tracking is fine-grained, so a change w","text":"effect() an effect runs a function and re-runs it whenever any reactive value it read during its last run changes. this is the heart of summit: every reactive directive is a kind of effect. tracking is fine-grained, so a change wakes only the effects that actually read the value that changed."},{"page":"effect()","category":"Reactivity","heading":"Running an effect","slug":"reactive-effect","anchor":"running-an-effect","snippet":"effect(fn) runs fn once immediately, recording which signals, computeds, and reactive properties it reads. When any of those change, fn runs again. Dependencies are recomputed on every run, so an effect that reads differ","text":"running an effect effect(fn) runs fn once immediately, recording which signals, computeds, and reactive properties it reads. when any of those change, fn runs again. dependencies are recomputed on every run, so an effect that reads different values on different runs subscribes to exactly what it read the last time."},{"page":"effect()","category":"Reactivity","heading":"Stopping an effect","slug":"reactive-effect","anchor":"stopping-an-effect","snippet":"effect() returns a runner. Pass it to stop() to dispose the effect permanently: it unsubscribes from every dependency and never runs again. You can also call the returned runner yourself to force a re-run.","text":"stopping an effect effect() returns a runner. pass it to stop() to dispose the effect permanently: it unsubscribes from every dependency and never runs again. you can also call the returned runner yourself to force a re-run."},{"page":"effect()","category":"Reactivity","heading":"Reading without subscribing","slug":"reactive-effect","anchor":"reading-without-subscribing","snippet":"Wrap a read in untrack() to use a value without depending on it. The effect still runs, but a later change to an untracked value will not trigger it.","text":"reading without subscribing wrap a read in untrack() to use a value without depending on it. the effect still runs, but a later change to an untracked value will not trigger it."},{"page":"effect()","category":"Reactivity","heading":"Writing a value the effect also reads","slug":"reactive-effect","anchor":"writing-a-value-the-effect-also-reads","snippet":"An effect that writes a signal it also reads will not trigger itself into a loop. The system skips re-running the effect that is currently executing, so this is safe.","text":"writing a value the effect also reads an effect that writes a signal it also reads will not trigger itself into a loop. the system skips re-running the effect that is currently executing, so this is safe."},{"page":"effect()","category":"Reactivity","heading":"Timing","slug":"reactive-effect","anchor":"timing","snippet":"By default an effect re-runs synchronously, the moment a dependency changes. The DOM-facing effects behind directives instead batch onto the microtask queue so a burst of writes produces a single update, which is why you","text":"timing by default an effect re-runs synchronously, the moment a dependency changes. the dom-facing effects behind directives instead batch onto the microtask queue so a burst of writes produces a single update, which is why you await nexttick() to read the updated dom. to coalesce several synchronous writes into one effect run yourself, use batch(). effect is also available on the global as summit.effect; stop and untrack ship as named exports of summitjs. for the full tracking model, see how reactivity works."},{"page":"nextTick()","category":"Reactivity","heading":"nextTick()","slug":"reactive-nexttick","anchor":"","snippet":"Reactive DOM updates do not happen synchronously on every write. Directives queue their work and flush once on the next microtask, so a burst of state changes produces a single coalesced update. nextTick() resolves after","text":"nexttick() reactive dom updates do not happen synchronously on every write. directives queue their work and flush once on the next microtask, so a burst of state changes produces a single coalesced update. nexttick() resolves after that flush, which is how you read the dom once it reflects your latest changes."},{"page":"nextTick()","category":"Reactivity","heading":"Awaiting updates","slug":"reactive-nexttick","anchor":"awaiting-updates","snippet":"Call nextTick() with no argument to get a promise that resolves after the pending flush.","text":"awaiting updates call nexttick() with no argument to get a promise that resolves after the pending flush."},{"page":"nextTick()","category":"Reactivity","heading":"With a callback","slug":"reactive-nexttick","anchor":"with-a-callback","snippet":"Pass a function and nextTick() runs it after the flush, then resolves. Either form returns a Promise<void, so you can await it or chain .then().","text":"with a callback pass a function and nexttick() runs it after the flush, then resolves. either form returns a promise<void, so you can await it or chain .then()."},{"page":"nextTick()","category":"Reactivity","heading":"The $nextTick magic","slug":"reactive-nexttick","anchor":"the-nexttick-magic","snippet":"Inside markup, the same capability is available as the $nextTick magic. It is this function bound into the expression scope, so you can wait for the DOM from an s- attribute: Use nextTick from JavaScript and $nextTick fr","text":"the $nexttick magic inside markup, the same capability is available as the $nexttick magic. it is this function bound into the expression scope, so you can wait for the dom from an s- attribute: use nexttick from javascript and $nexttick from an expression."},{"page":"nextTick()","category":"Reactivity","heading":"How the flush works","slug":"reactive-nexttick","anchor":"how-the-flush-works","snippet":"The batched, DOM-facing effects that directives are built on schedule themselves onto this same microtask queue, so awaiting nextTick after a state change is the reliable way to observe the resulting DOM. To collapse sev","text":"how the flush works the batched, dom-facing effects that directives are built on schedule themselves onto this same microtask queue, so awaiting nexttick after a state change is the reliable way to observe the resulting dom. to collapse several synchronous writes into one effect run before the flush, see batch(). for the queue and effect machinery underneath, see how reactivity works. nexttick is also available on the global as summit.nexttick."},{"page":"reactive()","category":"Reactivity","heading":"reactive()","slug":"reactive-reactive","anchor":"","snippet":"reactive(obj) wraps a plain object or array in a deep proxy that tracks reads and triggers on writes. It is what backs a component's s-data scope, so a plain assignment like state.open = true updates everything that read","text":"reactive() reactive(obj) wraps a plain object or array in a deep proxy that tracks reads and triggers on writes. it is what backs a component's s-data scope, so a plain assignment like state.open = true updates everything that read open. where a signal holds one value, reactive gives you an ordinary object you can read and mutate by property."},{"page":"reactive()","category":"Reactivity","heading":"Creating reactive state","slug":"reactive-reactive","anchor":"creating-reactive-state","snippet":"Reading a property subscribes the active effect to that property; writing it wakes the effects that read it. Adding or deleting a property also notifies code that iterates the object, for example an s-for.","text":"creating reactive state reading a property subscribes the active effect to that property; writing it wakes the effects that read it. adding or deleting a property also notifies code that iterates the object, for example an s-for."},{"page":"reactive()","category":"Reactivity","heading":"Deep and lazy","slug":"reactive-reactive","anchor":"deep-and-lazy","snippet":"Nested objects and arrays are reactive too. They are wrapped the first time you access them, not all at once up front.","text":"deep and lazy nested objects and arrays are reactive too. they are wrapped the first time you access them, not all at once up front."},{"page":"reactive()","category":"Reactivity","heading":"What gets wrapped","slug":"reactive-reactive","anchor":"what-gets-wrapped","snippet":"Only plain objects and arrays become reactive. Class instances, including DOM nodes, Date, Map, and RegExp, pass through untouched, so you can safely store a DOM element or other host object in reactive data without brea","text":"what gets wrapped only plain objects and arrays become reactive. class instances, including dom nodes, date, map, and regexp, pass through untouched, so you can safely store a dom element or other host object in reactive data without breaking it. reactive is idempotent: wrapping the same object twice returns the same proxy, and wrapping an existing proxy returns it unchanged."},{"page":"reactive()","category":"Reactivity","heading":"Unwrapping and detecting","slug":"reactive-reactive","anchor":"unwrapping-and-detecting","snippet":"toRaw(value) returns the original plain object behind a proxy. It is safe to call on a value that was never reactive, in which case it returns the value as-is.","text":"unwrapping and detecting toraw(value) returns the original plain object behind a proxy. it is safe to call on a value that was never reactive, in which case it returns the value as-is."},{"page":"reactive()","category":"Reactivity","heading":"How it fits","slug":"reactive-reactive","anchor":"how-it-fits","snippet":"reactive and signal() share one dependency-tracking core, so an effect() or computed() can depend on a mix of both. This proxy is exactly what gives s-data its ergonomics; see Reactivity and State for the HTML side and H","text":"how it fits reactive and signal() share one dependency-tracking core, so an effect() or computed() can depend on a mix of both. this proxy is exactly what gives s-data its ergonomics; see reactivity and state for the html side and how reactivity works for the internals. reactive is also available on the global as summit.reactive; toraw and isreactive ship as named exports of summitjs."},{"page":"signal()","category":"Reactivity","heading":"signal()","slug":"reactive-signal","anchor":"","snippet":"A signal is the smallest unit of reactive state in Summit: one value that keeps track of every effect that read it. Computed values, effects, and the deep reactive() proxy are all built on the same tracking core, so once","text":"signal() a signal is the smallest unit of reactive state in summit: one value that keeps track of every effect that read it. computed values, effects, and the deep reactive() proxy are all built on the same tracking core, so once you understand signals the rest of the system follows."},{"page":"signal()","category":"Reactivity","heading":"Creating a signal","slug":"reactive-signal","anchor":"creating-a-signal","snippet":"signal(initial) returns a signal that holds initial. The signal is a function, not an object with a .value property. You read it by calling it, and you write it with .set().","text":"creating a signal signal(initial) returns a signal that holds initial. the signal is a function, not an object with a .value property. you read it by calling it, and you write it with .set()."},{"page":"signal()","category":"Reactivity","heading":"Reading","slug":"reactive-signal","anchor":"reading","snippet":"Call the signal to get its current value. When you read a signal inside an effect() or computed(), that reader subscribes to the signal and re-runs whenever the value changes.","text":"reading call the signal to get its current value. when you read a signal inside an effect() or computed(), that reader subscribes to the signal and re-runs whenever the value changes."},{"page":"signal()","category":"Reactivity","heading":"Writing","slug":"reactive-signal","anchor":"writing","snippet":".set(next) replaces the value. Pass the next value directly, or a function that receives the previous value and returns the next one. A write only wakes subscribers when the value actually changes, compared with Object.i","text":"writing .set(next) replaces the value. pass the next value directly, or a function that receives the previous value and returns the next one. a write only wakes subscribers when the value actually changes, compared with object.is. setting a signal to the value it already holds does nothing."},{"page":"signal()","category":"Reactivity","heading":"Peeking without subscribing","slug":"reactive-signal","anchor":"peeking-without-subscribing","snippet":".peek() returns the current value without subscribing the active effect. Reach for it when you need the value but do not want a later change to trigger a re-run.","text":"peeking without subscribing .peek() returns the current value without subscribing the active effect. reach for it when you need the value but do not want a later change to trigger a re-run."},{"page":"signal()","category":"Reactivity","heading":"Checking for a signal","slug":"reactive-signal","anchor":"checking-for-a-signal","snippet":"isSignal(x) returns true for any value produced by signal(), and false otherwise. signal is also available on the global as Summit.signal. isSignal ships as a named export of summitjs.","text":"checking for a signal issignal(x) returns true for any value produced by signal(), and false otherwise. signal is also available on the global as summit.signal. issignal ships as a named export of summitjs."},{"page":"signal()","category":"Reactivity","heading":"How signals underpin the framework","slug":"reactive-signal","anchor":"how-signals-underpin-the-framework","snippet":"Every reactive s- directive ultimately reads and writes signals. The state you declare with s-data lives in a reactive() proxy, and both signals and that proxy feed the one dependency-tracking engine described in How rea","text":"how signals underpin the framework every reactive s- directive ultimately reads and writes signals. the state you declare with s-data lives in a reactive() proxy, and both signals and that proxy feed the one dependency-tracking engine described in how reactivity works. for the html-first way to hold and update state, see reactivity and state. to derive a cached value from a signal, reach for computed(); to run a side effect when it changes, use effect()."},{"page":"Reactivity and State","category":"Essentials","heading":"Reactivity and State","slug":"reactivity-state","anchor":"","snippet":"State in Summit lives in a component, declared with s-data. Everything inside that element can read and write the state, and every expression that reads a value is re-run automatically when that value changes.","text":"reactivity and state state in summit lives in a component, declared with s-data. everything inside that element can read and write the state, and every expression that reads a value is re-run automatically when that value changes."},{"page":"Reactivity and State","category":"Essentials","heading":"Declaring state","slug":"reactivity-state","anchor":"declaring-state","snippet":"Pass s-data an object literal. Its properties become reactive state. When you click, Summit knows the button's text read count, so it updates just that text node. Nothing else on the page is touched.","text":"declaring state pass s-data an object literal. its properties become reactive state. when you click, summit knows the button's text read count, so it updates just that text node. nothing else on the page is touched."},{"page":"Reactivity and State","category":"Essentials","heading":"Derived values","slug":"reactivity-state","anchor":"derived-values","snippet":"A getter is a value computed from other state. Summit caches it and only recomputes when something it depends on changes. full re-evaluates only when first or last change, and reading it twice in the same render does the","text":"derived values a getter is a value computed from other state. summit caches it and only recomputes when something it depends on changes. full re-evaluates only when first or last change, and reading it twice in the same render does the work once."},{"page":"Reactivity and State","category":"Essentials","heading":"Methods","slug":"reactivity-state","anchor":"methods","snippet":"Functions on the object are methods. Inside them, this is the component state, so you can read and mutate freely.","text":"methods functions on the object are methods. inside them, this is the component state, so you can read and mutate freely."},{"page":"Reactivity and State","category":"Essentials","heading":"Reusable components","slug":"reactivity-state","anchor":"reusable-components","snippet":"For anything beyond a small inline object, register a named provider with Summit.data and reference it by name. The provider is a function that returns the state object, so each use gets its own copy.","text":"reusable components for anything beyond a small inline object, register a named provider with summit.data and reference it by name. the provider is a function that returns the state object, so each use gets its own copy."},{"page":"Reactivity and State","category":"Essentials","heading":"Nested components","slug":"reactivity-state","anchor":"nested-components","snippet":"Components nest. An inner s-data can read its own state and everything from its ancestors, with the nearest name winning on a conflict.","text":"nested components components nest. an inner s-data can read its own state and everything from its ancestors, with the nearest name winning on a conflict."},{"page":"Reactivity and State","category":"Essentials","heading":"Shared state","slug":"reactivity-state","anchor":"shared-state","snippet":"State that many components need lives in a store, not in s-data. Reach it from any expression with the $store magic. See Summit.store to create one.","text":"shared state state that many components need lives in a store, not in s-data. reach it from any expression with the $store magic. see summit.store to create one."},{"page":"s-bind","category":"Directives","heading":"s-bind","slug":"s-bind","anchor":"","snippet":"s-bind sets an attribute from an expression and keeps it in sync as the data behind that expression changes. The shorthand is :, which is what you will normally write. :title=\"label\" is exactly s-bind:title=\"label\". When","text":"s-bind s-bind sets an attribute from an expression and keeps it in sync as the data behind that expression changes. the shorthand is :, which is what you will normally write. :title=\"label\" is exactly s-bind:title=\"label\". whenever label changes, the attribute updates."},{"page":"s-bind","category":"Directives","heading":"Boolean attributes","slug":"s-bind","anchor":"boolean-attributes","snippet":"For HTML boolean attributes such as disabled, checked, required, readonly, selected, open, hidden, and multiple, a truthy value adds the attribute and a falsy value removes it entirely. Summit also reflects the value to ","text":"boolean attributes for html boolean attributes such as disabled, checked, required, readonly, selected, open, hidden, and multiple, a truthy value adds the attribute and a falsy value removes it entirely. summit also reflects the value to the matching dom property, so form state stays correct. for a non-boolean attribute the rule is similar: false, null, and undefined remove the attribute, and every other value is set as a string."},{"page":"s-bind","category":"Directives","heading":":class","slug":"s-bind","anchor":"class","snippet":":class merges its result with whatever is already in the static class attribute, so you never lose your base classes. The value can be a string, an object, or an array. The object form is the common one. Each key is a cl","text":":class :class merges its result with whatever is already in the static class attribute, so you never lose your base classes. the value can be a string, an object, or an array. the object form is the common one. each key is a class name, applied while its value is truthy. the array form lists classes to apply, and its entries can themselves be strings or objects, which is handy for mixing always-on classes with conditional ones. because the static classes are always kept, put a class in class or in :class, not both, when you want to toggle it off."},{"page":"s-bind","category":"Directives","heading":":style","slug":"s-bind","anchor":"style","snippet":":style takes an object of CSS properties and merges it onto the static style attribute. Property names may be written camelCase or kebab-case, and any entry whose value is null or false is skipped. A string value works t","text":":style :style takes an object of css properties and merges it onto the static style attribute. property names may be written camelcase or kebab-case, and any entry whose value is null or false is skipped. a string value works too and is merged the same way."},{"page":"s-bind","category":"Directives","heading":"Binding an object of attributes","slug":"s-bind","anchor":"binding-an-object-of-attributes","snippet":"Given no attribute name, s-bind=\"obj\" applies a whole object at once. Plain entries become attributes, @-prefixed keys become event listeners, and :-prefixed keys become dynamic bindings whose function is re-run reactive","text":"binding an object of attributes given no attribute name, s-bind=\"obj\" applies a whole object at once. plain entries become attributes, @-prefixed keys become event listeners, and :-prefixed keys become dynamic bindings whose function is re-run reactively."},{"page":"s-bind","category":"Directives","heading":".camel","slug":"s-bind","anchor":"camel","snippet":"HTML attribute names are case-insensitive, which is a problem for SVG and other camelCase attributes. The .camel modifier converts the bound name from dashes to camelCase, so :view-box.camel sets viewBox.","text":".camel html attribute names are case-insensitive, which is a problem for svg and other camelcase attributes. the .camel modifier converts the bound name from dashes to camelcase, so :view-box.camel sets viewbox."},{"page":"s-cloak","category":"Directives","heading":"s-cloak","slug":"s-cloak","anchor":"","snippet":"Before Summit runs, your markup is just plain HTML: an s-text span is empty, an s-show element is fully visible, and bound attributes are unset. For a brief moment the page can show that raw, un-hydrated state. s-cloak h","text":"s-cloak before summit runs, your markup is just plain html: an s-text span is empty, an s-show element is fully visible, and bound attributes are unset. for a brief moment the page can show that raw, un-hydrated state. s-cloak hides an element until summit has finished initializing it."},{"page":"s-cloak","category":"Directives","heading":"The pattern","slug":"s-cloak","anchor":"the-pattern","snippet":"Add one CSS rule that hides anything carrying the attribute, then mark the elements you want kept out of sight until they are ready. When Summit initializes the element it removes the s-cloak attribute. The rule stops ma","text":"the pattern add one css rule that hides anything carrying the attribute, then mark the elements you want kept out of sight until they are ready. when summit initializes the element it removes the s-cloak attribute. the rule stops matching, and the element appears in its finished state with no flash of raw template in between. because the attribute is removed during setup, the rule only ever affects markup that has not been processed yet. the removal happens after the element's other directives have run, so by the time it becomes visible its text, bindings, and classes are already in place."},{"page":"s-cloak","category":"Directives","heading":"Where to put it","slug":"s-cloak","anchor":"where-to-put-it","snippet":"Put s-cloak on the outermost element whose contents you do not want to flash, which is usually the same element that carries s-data. You can also place it on individual pieces, such as a list that fills in from a fetch. ","text":"where to put it put s-cloak on the outermost element whose contents you do not want to flash, which is usually the same element that carries s-data. you can also place it on individual pieces, such as a list that fills in from a fetch. the css rule is yours to add once, globally. summit does not inject it for you, and there is nothing else to configure: the attribute is simply removed the first time the element initializes."},{"page":"s-data","category":"Directives","heading":"s-data","slug":"s-data","anchor":"","snippet":"s-data marks an element as a component and gives it state. Everything inside that element, its own directives and every descendant, can read and write that state, and Summit re-runs the expressions that depend on a value","text":"s-data s-data marks an element as a component and gives it state. everything inside that element, its own directives and every descendant, can read and write that state, and summit re-runs the expressions that depend on a value whenever the value changes."},{"page":"s-data","category":"Directives","heading":"Inline state","slug":"s-data","anchor":"inline-state","snippet":"Pass s-data an object literal. Its properties become reactive state that the whole subtree can use. An empty component is fine too. s-data on its own (or s-data=\"\") starts with an empty object, which is handy when the el","text":"inline state pass s-data an object literal. its properties become reactive state that the whole subtree can use. an empty component is fine too. s-data on its own (or s-data=\"\") starts with an empty object, which is handy when the element only needs an s-init or a place to hang $refs."},{"page":"s-data","category":"Directives","heading":"Getters","slug":"s-data","anchor":"getters","snippet":"A getter defines a value derived from other state. Summit turns each getter into a cached computed, so it recomputes only when something it read changes and returns the cached result the rest of the time. Read full in te","text":"getters a getter defines a value derived from other state. summit turns each getter into a cached computed, so it recomputes only when something it read changes and returns the cached result the rest of the time. read full in ten places and it still runs once per change to first or last."},{"page":"s-data","category":"Directives","heading":"Methods","slug":"s-data","anchor":"methods","snippet":"A function property is a method. Summit binds it to the component, so this is always the state whether you call it as double() or this.double(), and assigning to this.count triggers reactivity.","text":"methods a function property is a method. summit binds it to the component, so this is always the state whether you call it as double() or this.double(), and assigning to this.count triggers reactivity."},{"page":"s-data","category":"Directives","heading":"init()","slug":"s-data","anchor":"init","snippet":"If the state object has an init() method, Summit calls it once as the component is set up, before its children initialize. Use it to seed values, start a timer, or kick off a fetch. A matching destroy() method, if presen","text":"init() if the state object has an init() method, summit calls it once as the component is set up, before its children initialize. use it to seed values, start a timer, or kick off a fetch. a matching destroy() method, if present, runs when the component is torn down. inside init(), and inside any method, the $-magics are reachable through this, so this.$refs, this.$watch, and this.$el all work."},{"page":"s-data","category":"Directives","heading":"Named providers","slug":"s-data","anchor":"named-providers","snippet":"For anything past a small inline object, register the state once with Summit.data and reference it by name. The provider is a function that returns the state object, so every element that uses it gets a fresh copy. Call ","text":"named providers for anything past a small inline object, register the state once with summit.data and reference it by name. the provider is a function that returns the state object, so every element that uses it gets a fresh copy. call it with arguments right in the attribute. the name is matched first against your registered providers, and the arguments are evaluated in the surrounding scope."},{"page":"s-data","category":"Directives","heading":"Nesting and scope","slug":"s-data","anchor":"nesting-and-scope","snippet":"Components nest freely. An inner s-data sees its own state plus everything from its ancestors, and the nearest name wins when two scopes define the same key. State that many unrelated components share belongs in a store ","text":"nesting and scope components nest freely. an inner s-data sees its own state plus everything from its ancestors, and the nearest name wins when two scopes define the same key. state that many unrelated components share belongs in a store rather than s-data. create one with summit.store and reach it from any expression with $store."},{"page":"s-effect","category":"Directives","heading":"s-effect","slug":"s-effect","anchor":"","snippet":"s-effect runs an expression as a reactive side effect. It runs once when the element initializes, then re-runs automatically whenever any state it read changes. Summit tracks those reads for you, so there is no dependenc","text":"s-effect s-effect runs an expression as a reactive side effect. it runs once when the element initializes, then re-runs automatically whenever any state it read changes. summit tracks those reads for you, so there is no dependency list to maintain. each click reads count, so summit re-runs the effect and the tab title updates. re-runs are batched onto the microtask queue, so several changes in the same tick collapse into a single run."},{"page":"s-effect","category":"Directives","heading":"When to use it","slug":"s-effect","anchor":"when-to-use-it","snippet":"Use s-effect for side effects that reach outside the reactive graph: writing to document.title, syncing to localStorage, logging, or driving a non-Summit library. The expression runs for what it does, not for a value it ","text":"when to use it use s-effect for side effects that reach outside the reactive graph: writing to document.title, syncing to localstorage, logging, or driving a non-summit library. the expression runs for what it does, not for a value it returns. to put a value on screen you do not need s-effect. bind it directly with s-text or s-bind, or derive it with a getter, and let that directive track its own dependencies. reserve s-effect for the cases a plain binding cannot express. to respond to one specific value changing rather than everything an expression touches, see $watch. to run something once at startup with no re-runs, use s-init."},{"page":"s-for","category":"Directives","heading":"s-for","slug":"s-for","anchor":"","snippet":"s-for renders one copy of a template for each item in a collection. It lives on a <template element and, with a :key, reconciles the list by key so existing nodes are moved rather than rebuilt.","text":"s-for s-for renders one copy of a template for each item in a collection. it lives on a <template element and, with a :key, reconciles the list by key so existing nodes are moved rather than rebuilt."},{"page":"s-for","category":"Directives","heading":"Rendering a list","slug":"s-for","anchor":"rendering-a-list","snippet":"Put s-for on a <template that wraps a single root element. Summit renders that element once per item and inserts the results where the template stood. The template must be a <template tag with exactly one root child. Sum","text":"rendering a list put s-for on a <template that wraps a single root element. summit renders that element once per item and inserts the results where the template stood. the template must be a <template tag with exactly one root child. summit warns in the console if either is missing."},{"page":"s-for","category":"Directives","heading":"Item and index","slug":"s-for","anchor":"item-and-index","snippet":"Use item in items for just the value, or (item, index) in items when you also need the position. Each iteration gets its own scope holding item (and index if named). Those blocks can still read state from ancestor compon","text":"item and index use item in items for just the value, or (item, index) in items when you also need the position. each iteration gets its own scope holding item (and index if named). those blocks can still read state from ancestor components as usual. both in and of are accepted as the separator."},{"page":"s-for","category":"Directives","heading":"Iterating a range","slug":"s-for","anchor":"iterating-a-range","snippet":"Give s-for a number and it counts from 1 up to that number. star in 5 yields the values 1, 2, 3, 4, 5.","text":"iterating a range give s-for a number and it counts from 1 up to that number. star in 5 yields the values 1, 2, 3, 4, 5."},{"page":"s-for","category":"Directives","heading":"Iterating an object","slug":"s-for","anchor":"iterating-an-object","snippet":"Pass an object and (value, key) in object gives you each entry. The second name receives the property key.","text":"iterating an object pass an object and (value, key) in object gives you each entry. the second name receives the property key."},{"page":"s-for","category":"Directives","heading":"Keyed reconciliation","slug":"s-for","anchor":"keyed-reconciliation","snippet":"Add :key (or s-bind:key) with an expression that is unique per item. When the list changes, Summit matches items by key: a block whose key already exists is kept and moved to its new position rather than destroyed and re","text":"keyed reconciliation add :key (or s-bind:key) with an expression that is unique per item. when the list changes, summit matches items by key: a block whose key already exists is kept and moved to its new position rather than destroyed and recreated. that preserves dom state such as focus, scroll, and uncommitted input values across reorders. type into an input, then reverse the list: because each row is matched by person.id and moved, your text stays with its person. without a :key, summit falls back to the object key for objects, or the numeric index otherwise, which rebuilds rows when their position changes and loses that state. to conditionally render a single element rather than a list, see s-if."},{"page":"s-html","category":"Directives","heading":"s-html","slug":"s-html","anchor":"","snippet":"s-html sets an element's innerHTML to the value of an expression and re-runs when that value changes. Reach for it when the content itself is markup, for example a fragment rendered on the server or returned from a fetch","text":"s-html s-html sets an element's innerhtml to the value of an expression and re-runs when that value changes. reach for it when the content itself is markup, for example a fragment rendered on the server or returned from a fetch. unlike s-text, the string is parsed as html, so the <strong above becomes real bold text."},{"page":"s-html","category":"Directives","heading":"Injected markup is initialized","slug":"s-html","anchor":"injected-markup-is-initialized","snippet":"After setting the HTML, Summit walks the new children and initializes any Summit directives it finds, using the surrounding component's scope. A fragment that arrives with s-text or @click on it becomes reactive right aw","text":"injected markup is initialized after setting the html, summit walks the new children and initializes any summit directives it finds, using the surrounding component's scope. a fragment that arrives with s-text or @click on it becomes reactive right away, with no extra step."},{"page":"s-html","category":"Directives","heading":"Only use it on trusted content","slug":"s-html","anchor":"only-use-it-on-trusted-content","snippet":"Setting innerHTML from untrusted input is an XSS vulnerability. A string containing a <script tag or an onerror handler can run code in your page. Never point s-html at anything a user typed, a URL parameter, or a third-","text":"only use it on trusted content setting innerhtml from untrusted input is an xss vulnerability. a string containing a <script tag or an onerror handler can run code in your page. never point s-html at anything a user typed, a url parameter, or a third-party response you do not control. if the source is not fully trusted, use s-text instead, which escapes everything it renders."},{"page":"s-if","category":"Directives","heading":"s-if","slug":"s-if","anchor":"","snippet":"s-if adds an element to the DOM when its expression is truthy and removes it when it is falsy. Unlike hiding with display, the element genuinely comes and goes.","text":"s-if s-if adds an element to the dom when its expression is truthy and removes it when it is falsy. unlike hiding with display, the element genuinely comes and goes."},{"page":"s-if","category":"Directives","heading":"Conditionally rendering","slug":"s-if","anchor":"conditionally-rendering","snippet":"Put s-if on the element you want to appear and disappear. Behind the scenes Summit leaves a comment marker where the element used to be. When the condition turns true it clones the element in at that spot and initializes","text":"conditionally rendering put s-if on the element you want to appear and disappear. behind the scenes summit leaves a comment marker where the element used to be. when the condition turns true it clones the element in at that spot and initializes it; when the condition turns false it tears the element down and removes it. because the node is rebuilt each time it returns, its internal state does not carry over between removals."},{"page":"s-if","category":"Directives","heading":"Works on any element","slug":"s-if","anchor":"works-on-any-element","snippet":"You do not need a <template wrapper. s-if works directly on any element, and the element itself (minus its s-if attribute) is the blueprint that gets cloned in and out. A <template still works if you prefer it. In that c","text":"works on any element you do not need a <template wrapper. s-if works directly on any element, and the element itself (minus its s-if attribute) is the blueprint that gets cloned in and out. a <template still works if you prefer it. in that case its single root child is used as the blueprint:"},{"page":"s-if","category":"Directives","heading":"Difference from s-show","slug":"s-if","anchor":"difference-from-s-show","snippet":"s-if and s-show look similar but differ in what they do to the element: - s-if adds and removes the node. While the condition is false the element is not in the DOM at all and its reactive effects are destroyed. Returnin","text":"difference from s-show s-if and s-show look similar but differ in what they do to the element: - s-if adds and removes the node. while the condition is false the element is not in the dom at all and its reactive effects are destroyed. returning to true rebuilds it from scratch. - s-show always keeps the node in the dom and only toggles its display. state inside it survives being hidden. use s-if when you want the element and its cost gone entirely, or when it is rarely shown. use s-show when you toggle frequently or need to preserve the element's internal state. for lists of elements that come and go, see s-for, which reconciles by key. to animate elements as they appear and disappear, pair s-transition with s-show."},{"page":"s-ignore","category":"Directives","heading":"s-ignore","slug":"s-ignore","anchor":"","snippet":"s-ignore tells Summit to leave an element and everything inside it alone. Summit does not read its directives, does not descend into its children, and sets up no reactivity anywhere in that subtree.","text":"s-ignore s-ignore tells summit to leave an element and everything inside it alone. summit does not read its directives, does not descend into its children, and sets up no reactivity anywhere in that subtree."},{"page":"s-ignore","category":"Directives","heading":"Skipping a subtree","slug":"s-ignore","anchor":"skipping-a-subtree","snippet":"When Summit reaches an element marked s-ignore, it stops there and moves on. Any directives inside the ignored region are left untouched. The first span shows Ada. The second sits inside an s-ignore block, so its s-text ","text":"skipping a subtree when summit reaches an element marked s-ignore, it stops there and moves on. any directives inside the ignored region are left untouched. the first span shows ada. the second sits inside an s-ignore block, so its s-text never runs and the span stays empty. the skip covers the whole subtree, not just the element itself."},{"page":"s-ignore","category":"Directives","heading":"When to use it","slug":"s-ignore","anchor":"when-to-use-it","snippet":"- Third-party widgets that manage their own DOM, such as a map, a rich text editor, or a payment field. Wrapping the mount point in s-ignore keeps Summit from walking into markup it does not own and does not understand. ","text":"when to use it - third-party widgets that manage their own dom, such as a map, a rich text editor, or a payment field. wrapping the mount point in s-ignore keeps summit from walking into markup it does not own and does not understand. - server-rendered islands and large static regions where you simply want to skip the initialization walk. s-ignore is a hard stop for the entire subtree, not a per-directive opt-out. if you need reactivity on part of that region, keep it outside the ignored element. where s-cloak hides markup until it is ready, s-ignore never initializes it at all."},{"page":"s-init","category":"Directives","heading":"s-init","slug":"s-init","anchor":"","snippet":"s-init runs an expression once, at the moment its element is set up. It is the quick, inline way to do a bit of startup work without writing an init() method on your data.","text":"s-init s-init runs an expression once, at the moment its element is set up. it is the quick, inline way to do a bit of startup work without writing an init() method on your data."},{"page":"s-init","category":"Directives","heading":"Running once at startup","slug":"s-init","anchor":"running-once-at-startup","snippet":"Put the expression right on the element. It runs a single time as Summit initializes the element, before any children below it are set up. Unlike s-effect, which re-runs whenever the state it reads changes, s-init fires ","text":"running once at startup put the expression right on the element. it runs a single time as summit initializes the element, before any children below it are set up. unlike s-effect, which re-runs whenever the state it reads changes, s-init fires exactly once and never again. reach for it when you want a one-time action, not a reactive one."},{"page":"s-init","category":"Directives","heading":"It works on any element","slug":"s-init","anchor":"it-works-on-any-element","snippet":"s-init is an ordinary directive, so it is not limited to a component root. You can attach it to any element inside a component, and each one runs as that element initializes.","text":"it works on any element s-init is an ordinary directive, so it is not limited to a component root. you can attach it to any element inside a component, and each one runs as that element initializes."},{"page":"s-init","category":"Directives","heading":"Relationship to a component's init() method","slug":"s-init","anchor":"relationship-to-a-components-init-method","snippet":"A component defined with s-data can expose an init() method, which Summit calls when the component is created (see Lifecycle). The two are complementary: - Use init() for setup that is substantial or belongs alongside th","text":"relationship to a component's init() method a component defined with s-data can expose an init() method, which summit calls when the component is created (see lifecycle). the two are complementary: - use init() for setup that is substantial or belongs alongside the component's data, such as fetching, reading storage, or starting a timer. - use s-init for a short expression on a single element, including elements that do not own any data of their own. when one element carries both an s-data init() method and an s-init directive, the init() method runs first, then the s-init expression."},{"page":"s-model","category":"Directives","heading":"s-model","slug":"s-model","anchor":"","snippet":"s-model wires a form control to a piece of state in both directions. When the user changes the control, the state updates; when the state changes from anywhere else, the control updates to match. It replaces the pair of ","text":"s-model s-model wires a form control to a piece of state in both directions. when the user changes the control, the state updates; when the state changes from anywhere else, the control updates to match. it replaces the pair of an s-bind on value and an s-on input listener that you would otherwise write by hand."},{"page":"s-model","category":"Directives","heading":"How it stays in sync","slug":"s-model","anchor":"how-it-stays-in-sync","snippet":"Two things run under the hood. A reactive effect writes the current state into the control whenever the state changes, and an event listener reads the control back into the state whenever the user interacts with it. The ","text":"how it stays in sync two things run under the hood. a reactive effect writes the current state into the control whenever the state changes, and an event listener reads the control back into the state whenever the user interacts with it. the read and write both know which property matters for the element, so the same directive covers every control type. the listener defaults to the input event for text-like fields and switches to change for select, checkbox, and radio elements."},{"page":"s-model","category":"Directives","heading":"Every control type","slug":"s-model","anchor":"every-control-type","snippet":"s-model inspects the element and picks the right value to read and write: - Text, textarea, number, range: bound to the control's value. - Checkbox: a boolean when one checkbox is bound on its own, or an array of the che","text":"every control type s-model inspects the element and picks the right value to read and write: - text, textarea, number, range: bound to the control's value. - checkbox: a boolean when one checkbox is bound on its own, or an array of the checked controls' value attributes when several checkboxes share the same model. - radio: the value of the selected control in the group. - select: the chosen option's value, or an array of selected values when the select has the multiple attribute."},{"page":"s-model","category":"Directives","heading":"Modifiers","slug":"s-model","anchor":"modifiers","snippet":".number and .boolean apply when the value is read from text-like fields, radios, and single selects. .debounce defaults to 250ms and takes a custom duration as the next modifier, the same as it does on s-on: ms is millis","text":"modifiers .number and .boolean apply when the value is read from text-like fields, radios, and single selects. .debounce defaults to 250ms and takes a custom duration as the next modifier, the same as it does on s-on: ms is milliseconds, s is seconds."},{"page":"s-model","category":"Directives","heading":"Filling from markup","slug":"s-model","anchor":"filling-from-markup","snippet":".fill is useful when the initial value lives in the HTML rather than in s-data. On setup, if the bound state is undefined, null, or an empty string, s-model.fill reads the control's current value (running it through any ","text":"filling from markup .fill is useful when the initial value lives in the html rather than in s-data. on setup, if the bound state is undefined, null, or an empty string, s-model.fill reads the control's current value (running it through any .number or .boolean coercion) and seeds the state with it."},{"page":"s-model","category":"Directives","heading":"Building custom inputs","slug":"s-model","anchor":"building-custom-inputs","snippet":"Every element with s-model gets an summitModel property assigned to it, an object with get and set functions that read and write the bound state: A wrapper component can use this to drive the binding from a non-standard ","text":"building custom inputs every element with s-model gets an summitmodel property assigned to it, an object with get and set functions that read and write the bound state: a wrapper component can use this to drive the binding from a non-standard control. put s-model on the wrapper, then have the custom widget call summitmodel.set(...) when its value changes and read summitmodel.get() to render, and the two-way binding works exactly as it does for native inputs. for the underlying value binding this builds on, see s-bind, and for the form patterns it fits into, see forms."},{"page":"s-on","category":"Directives","heading":"s-on","slug":"s-on","anchor":"","snippet":"s-on attaches an event listener to an element and runs an expression each time the event fires. You write it as s-on:click=\"...\", but the @ shorthand is what you will reach for almost every time: @click and s-on:click ar","text":"s-on s-on attaches an event listener to an element and runs an expression each time the event fires. you write it as s-on:click=\"...\", but the @ shorthand is what you will reach for almost every time: @click and s-on:click are exactly the same directive. the part after the colon (or the @) is the event name, and everything after the first dot is a modifier."},{"page":"s-on","category":"Directives","heading":"The event object","slug":"s-on","anchor":"the-event-object","snippet":"The native event is available inside the expression as $event, so you can read the target, the pressed key, coordinates, or anything else off it. If your expression evaluates to a function rather than running inline, Sum","text":"the event object the native event is available inside the expression as $event, so you can read the target, the pressed key, coordinates, or anything else off it. if your expression evaluates to a function rather than running inline, summit calls that function for you and passes the event as its argument. that means a bare method reference works without writing out the call: here handleclick receives the event, the same as if you had written @click=\"handleclick($event)\"."},{"page":"s-on","category":"Directives","heading":"Modifiers","slug":"s-on","anchor":"modifiers","snippet":"Modifiers are dot-suffixed flags on the directive name that change how the listener behaves. They stack, and apart from a timing duration (which has to follow its .debounce or .throttle) the order does not matter.","text":"modifiers modifiers are dot-suffixed flags on the directive name that change how the listener behaves. they stack, and apart from a timing duration (which has to follow its .debounce or .throttle) the order does not matter."},{"page":"s-on","category":"Directives","heading":"Event control","slug":"s-on","anchor":"event-control","snippet":"These adjust dispatch and registration of the listener itself.","text":"event control these adjust dispatch and registration of the listener itself."},{"page":"s-on","category":"Directives","heading":"Targets","slug":"s-on","anchor":"targets","snippet":"By default the listener lives on the element itself. Two modifiers move it elsewhere, which is how you register global shortcuts and dismiss-on-outside behavior: - .window attaches the listener to window. - .document att","text":"targets by default the listener lives on the element itself. two modifiers move it elsewhere, which is how you register global shortcuts and dismiss-on-outside behavior: - .window attaches the listener to window. - .document attaches it to document. - .outside also listens on document, then ignores any event whose target is this element or one of its descendants. events on nodes that have already been removed from the page are ignored too, so tearing down an element does not trigger it. a window-scoped keyboard shortcut looks like this:"},{"page":"s-on","category":"Directives","heading":"Key filters","slug":"s-on","anchor":"key-filters","snippet":"On a keyboard event you can restrict the listener to specific keys by adding the key name as a modifier. Summit recognizes these aliases and maps each to the matching KeyboardEvent.key value: Any key that is not in the t","text":"key filters on a keyboard event you can restrict the listener to specific keys by adding the key name as a modifier. summit recognizes these aliases and maps each to the matching keyboardevent.key value: any key that is not in the table matches by its own name, compared case-insensitively (and with camelcase keys treated as their dashed form). so .k matches the k key and .a matches a. when you list more than one key filter, the listener fires if the event matches any of them."},{"page":"s-on","category":"Directives","heading":"System keys","slug":"s-on","anchor":"system-keys","snippet":"System modifiers require a held modifier key. Unlike key filters, these are combined with AND, so every one you list must be down when the event fires. Combine them with a key filter to build shortcuts. This one fires on","text":"system keys system modifiers require a held modifier key. unlike key filters, these are combined with and, so every one you list must be down when the event fires. combine them with a key filter to build shortcuts. this one fires only on cmd (or the meta key) plus k, and blocks the browser default:"},{"page":"s-on","category":"Directives","heading":"Timing","slug":"s-on","anchor":"timing","snippet":".debounce waits until events stop arriving before running your expression, and .throttle runs it at most once per interval. Both default to 250ms. To set a custom duration, add it as the very next modifier after the timi","text":"timing .debounce waits until events stop arriving before running your expression, and .throttle runs it at most once per interval. both default to 250ms. to set a custom duration, add it as the very next modifier after the timing flag. a bare number and a ms suffix are milliseconds; an s suffix is seconds."},{"page":"s-on","category":"Directives","heading":"Name transforms","slug":"s-on","anchor":"name-transforms","snippet":"Because HTML lowercases attribute names, two modifiers help you target events whose names contain capitals or dots: - .camel converts a dashed event name to camelCase, so @custom-event.camel listens for a customEvent eve","text":"name transforms because html lowercases attribute names, two modifiers help you target events whose names contain capitals or dots: - .camel converts a dashed event name to camelcase, so @custom-event.camel listens for a customevent event. - .dot converts dashes back to dots, so @my-event.dot listens for a my.event event. to send events that these modifiers can catch, use $dispatch. for the state that these listeners usually update, see s-data and reactivity."},{"page":"s-ref","category":"Directives","heading":"s-ref","slug":"s-ref","anchor":"","snippet":"s-ref gives an element a name so you can reach the real DOM node later through the $refs magic. This is how you focus an input, measure a box, or hand a node to another library, without reaching for document.querySelecto","text":"s-ref s-ref gives an element a name so you can reach the real dom node later through the $refs magic. this is how you focus an input, measure a box, or hand a node to another library, without reaching for document.queryselector."},{"page":"s-ref","category":"Directives","heading":"Naming and reaching an element","slug":"s-ref","anchor":"naming-and-reaching-an-element","snippet":"The value of s-ref is the name. That name becomes a key on $refs, and its value is the element itself. Refs are registered on the nearest component root, so every element inside the same s-data component shares one $refs","text":"naming and reaching an element the value of s-ref is the name. that name becomes a key on $refs, and its value is the element itself. refs are registered on the nearest component root, so every element inside the same s-data component shares one $refs registry. when the element leaves the dom, its ref is removed for you."},{"page":"s-ref","category":"Directives","heading":"Dynamic names","slug":"s-ref","anchor":"dynamic-names","snippet":"If the expression resolves to a non-empty string, Summit uses that string as the name. Otherwise it falls back to the literal text you wrote. That means a plain name like s-ref=\"field\" just works, and you can also comput","text":"dynamic names if the expression resolves to a non-empty string, summit uses that string as the name. otherwise it falls back to the literal text you wrote. that means a plain name like s-ref=\"field\" just works, and you can also compute a name, which is handy inside an s-for loop. here each row registers under row-1, row-2, and so on, reachable as $refs['row-' + id]. one thing to watch: because the value is evaluated, choosing a static name that also happens to be a string variable in scope will use that variable's value as the name instead of the literal text."},{"page":"s-ref","category":"Directives","heading":"Reaching a ref after the DOM updates","slug":"s-ref","anchor":"reaching-a-ref-after-the-dom-updates","snippet":"If you reveal an element and then want to focus it in the same handler, wait for the DOM to catch up first with $nextTick.","text":"reaching a ref after the dom updates if you reveal an element and then want to focus it in the same handler, wait for the dom to catch up first with $nexttick."},{"page":"s-show","category":"Directives","heading":"s-show","slug":"s-show","anchor":"","snippet":"s-show shows or hides an element by setting its inline display. The element never leaves the DOM: it stays parsed, initialized, and keeps its state. Only its visibility flips.","text":"s-show s-show shows or hides an element by setting its inline display. the element never leaves the dom: it stays parsed, initialized, and keeps its state. only its visibility flips."},{"page":"s-show","category":"Directives","heading":"Toggling visibility","slug":"s-show","anchor":"toggling-visibility","snippet":"Give s-show any expression. When it is truthy the element is visible, when it is falsy it is hidden with display: none. When the expression becomes falsy, Summit sets display: none. When it becomes truthy again, Summit r","text":"toggling visibility give s-show any expression. when it is truthy the element is visible, when it is falsy it is hidden with display: none. when the expression becomes falsy, summit sets display: none. when it becomes truthy again, summit restores whatever display the element had to begin with. if you started the element at display: flex, that value comes back; if it had no inline display, the property is removed so your css or the browser default takes over."},{"page":"s-show","category":"Directives","heading":"Beating stubborn CSS","slug":"s-show","anchor":"beating-stubborn-css","snippet":"If another rule forces the element to display, add the .important modifier so Summit hides it with display: none !important.","text":"beating stubborn css if another rule forces the element to display, add the .important modifier so summit hides it with display: none !important."},{"page":"s-show","category":"Directives","heading":"Difference from s-if","slug":"s-show","anchor":"difference-from-s-if","snippet":"Both hide things, but they do it at different levels: - s-show keeps the element in the DOM and only changes display. Toggling is cheap, and any internal state (input values, scroll position, child component state) is pr","text":"difference from s-if both hide things, but they do it at different levels: - s-show keeps the element in the dom and only changes display. toggling is cheap, and any internal state (input values, scroll position, child component state) is preserved while hidden. - s-if adds and removes the element itself. when the condition is false the node is gone from the dom and its reactive effects are torn down; when it returns it is built fresh. reach for s-show when you toggle often or need to keep state alive. reach for s-if when the element is expensive or rarely shown and you want it fully gone."},{"page":"s-show","category":"Directives","heading":"Working with transitions","slug":"s-show","anchor":"working-with-transitions","snippet":"Pair s-show with s-transition to animate the element as it appears and disappears. The initial render is applied instantly; every toggle after that runs the enter or leave animation.","text":"working with transitions pair s-show with s-transition to animate the element as it appears and disappears. the initial render is applied instantly; every toggle after that runs the enter or leave animation."},{"page":"s-teleport","category":"Directives","heading":"s-teleport","slug":"s-teleport","anchor":"","snippet":"s-teleport renders content somewhere else in the document while keeping it wired to the component it was written in. It is what you use for modals, popovers, and toasts that need to escape a parent's overflow or stacking","text":"s-teleport s-teleport renders content somewhere else in the document while keeping it wired to the component it was written in. it is what you use for modals, popovers, and toasts that need to escape a parent's overflow or stacking context."},{"page":"s-teleport","category":"Directives","heading":"A template that renders elsewhere","slug":"s-teleport","anchor":"a-template-that-renders-elsewhere","snippet":"s-teleport goes on a <template element. Its value is a CSS selector, resolved with document.querySelector. Summit clones the template's content, inserts the clones at the target, and initializes them with the current com","text":"a template that renders elsewhere s-teleport goes on a <template element. its value is a css selector, resolved with document.queryselector. summit clones the template's content, inserts the clones at the target, and initializes them with the current component's scope, so the moved markup can still read and write the component's state. the modal markup is authored inside the component, yet it ends up appended to <body, clear of any overflow: hidden ancestor. because summit initializes it with the component's scope, open still controls it with s-show and the close button still writes back."},{"page":"s-teleport","category":"Directives","heading":"Choosing where it lands","slug":"s-teleport","anchor":"choosing-where-it-lands","snippet":"By default the content is placed inside the target, as its last child. Two modifiers place it relative to the target instead. So s-teleport=\"body\" puts the content at the end of the page body, while s-teleport.append=\"an","text":"choosing where it lands by default the content is placed inside the target, as its last child. two modifiers place it relative to the target instead. so s-teleport=\"body\" puts the content at the end of the page body, while s-teleport.append=\"anchor\" drops it in right after the element with id anchor."},{"page":"s-teleport","category":"Directives","heading":"Notes","slug":"s-teleport","anchor":"notes","snippet":"- The directive only works on a <template element. On anything else Summit logs a warning and does nothing. - The selector must match an element that already exists when the template initializes. If the target is not fou","text":"notes - the directive only works on a <template element. on anything else summit logs a warning and does nothing. - the selector must match an element that already exists when the template initializes. if the target is not found, summit warns and renders nothing. - only the template's children are moved. the <template element itself stays where you wrote it and shows nothing. - when the component or the template is removed, summit tears down and removes the teleported nodes too, so no detached dom is left behind. to animate the teleported content as it appears, pair it with s-transition."},{"page":"s-text","category":"Directives","heading":"s-text","slug":"s-text","anchor":"","snippet":"s-text sets an element's text content to the value of an expression and updates it whenever the data behind that expression changes. The element's existing content is replaced, so you can give it fallback text to show be","text":"s-text s-text sets an element's text content to the value of an expression and updates it whenever the data behind that expression changes. the element's existing content is replaced, so you can give it fallback text to show before summit loads. it is swapped out on init."},{"page":"s-text","category":"Directives","heading":"Any expression","slug":"s-text","anchor":"any-expression","snippet":"The value is a full expression, not just a property name, so format it inline. null and undefined render as an empty string. Every other value is turned into a string.","text":"any expression the value is a full expression, not just a property name, so format it inline. null and undefined render as an empty string. every other value is turned into a string."},{"page":"s-text","category":"Directives","heading":"Text is escaped","slug":"s-text","anchor":"text-is-escaped","snippet":"s-text writes through textContent, so whatever the expression returns shows as literal text. HTML in the value is never parsed, which makes s-text safe for user-supplied strings. The angle brackets appear on screen inste","text":"text is escaped s-text writes through textcontent, so whatever the expression returns shows as literal text. html in the value is never parsed, which makes s-text safe for user-supplied strings. the angle brackets appear on screen instead of creating an <em. when you deliberately want markup rendered, and only for content you trust, reach for s-html."},{"page":"s-transition","category":"Directives","heading":"s-transition","slug":"s-transition","anchor":"","snippet":"s-transition animates an element as it becomes visible and as it hides. It comes in two flavors: a class style, where you supply the CSS classes for each phase, and a helper style, where a few modifiers produce a default","text":"s-transition s-transition animates an element as it becomes visible and as it hides. it comes in two flavors: a class style, where you supply the css classes for each phase, and a helper style, where a few modifiers produce a default fade and scale with no css to write."},{"page":"s-transition","category":"Directives","heading":"How it pairs with s-show","slug":"s-transition","anchor":"how-it-pairs-with-s-show","snippet":"Transitions run through s-show. Put both directives on the same element: s-show decides when the element is visible, and s-transition animates the change. The very first render is applied instantly with no animation. Eve","text":"how it pairs with s-show transitions run through s-show. put both directives on the same element: s-show decides when the element is visible, and s-transition animates the change. the very first render is applied instantly with no animation. every toggle after that plays the enter animation when the element appears and the leave animation when it hides, and the element is only set to display: none once the leave animation finishes. note that s-if does not run transitions. it adds and removes the node directly, so to animate an element in and out, use s-show rather than s-if."},{"page":"s-transition","category":"Directives","heading":"Class style","slug":"s-transition","anchor":"class-style","snippet":"Supply a class list for each phase after a colon. There are six phases, three for entering and three for leaving: - s-transition:enter stays applied for the whole enter animation. - s-transition:enter-start is the starti","text":"class style supply a class list for each phase after a colon. there are six phases, three for entering and three for leaving: - s-transition:enter stays applied for the whole enter animation. - s-transition:enter-start is the starting look, applied before the first frame. - s-transition:enter-end is the target look, applied on the next frame. - s-transition:leave, s-transition:leave-start, and s-transition:leave-end mirror those three for leaving. on enter, summit adds the enter and enter-start classes, then on the next frame removes enter-start and adds enter-end, so the element transitions from the start look to the end look. when the animation is done it removes the enter and enter-end classes. leaving works the same way with the leave classes. the animation's duration is read from the element's computed transition-duration, so define your transition on the class that stays applied through the phase, that is enter and leave. if no css transition duration is found, summit falls back to 150ms."},{"page":"s-transition","category":"Directives","heading":"Helper style","slug":"s-transition","anchor":"helper-style","snippet":"Use s-transition with no phase and let modifiers describe the animation with inline styles instead of CSS classes: - .opacity fades the element. - .scale scales it. Follow it with a number to set the scale as a percent, ","text":"helper style use s-transition with no phase and let modifiers describe the animation with inline styles instead of css classes: - .opacity fades the element. - .scale scales it. follow it with a number to set the scale as a percent, for example .scale.90 scales from 90%. on its own it uses 95%. - .duration sets the time, for example .duration.300ms or .duration.300. - .delay waits before starting, for example .delay.100ms. a bare s-transition with no modifiers defaults to a fade combined with a scale from 95%, over 150ms. because the helper style writes its own inline styles, it needs no accompanying css to be visible."},{"page":"Getting Started","category":"Getting Started","heading":"Getting Started","slug":"start","anchor":"","snippet":"Summit is a small JavaScript framework for composing behavior directly in your markup. You sprinkle a handful of s- attributes onto plain HTML and Summit brings it to life. No build step, no components to import, no JSX.","text":"getting started summit is a small javascript framework for composing behavior directly in your markup. you sprinkle a handful of s- attributes onto plain html and summit brings it to life. no build step, no components to import, no jsx. if you know html, you already know most of summit. under the hood it runs on a fine-grained signal engine, so when a value changes summit updates only the exact piece of dom that depends on it. expressions are parsed and interpreted rather than handed to eval, so summit works under a strict content-security-policy with nothing extra to configure."},{"page":"Getting Started","category":"Getting Started","heading":"Your first component","slug":"start","anchor":"your-first-component","snippet":"Here is a complete, working counter. There is no setup around it: this snippet is running live on the page right now. s-data declares a piece of state. @click (shorthand for s-on:click) runs an expression when the button","text":"your first component here is a complete, working counter. there is no setup around it: this snippet is running live on the page right now. s-data declares a piece of state. @click (shorthand for s-on:click) runs an expression when the button is clicked. s-text keeps the <output in sync with count. when you click, only that one number re-renders."},{"page":"Getting Started","category":"Getting Started","heading":"Why Summit","slug":"start","anchor":"why-summit","snippet":"- Fine-grained reactivity. A real signal engine tracks exactly which expressions read which values, so updates are surgical rather than re-rendering whole subtrees. - CSP-safe by default. Expressions run through a hand-w","text":"why summit - fine-grained reactivity. a real signal engine tracks exactly which expressions read which values, so updates are surgical rather than re-rendering whole subtrees. - csp-safe by default. expressions run through a hand-written interpreter. no eval, no new function, no unsafe-eval in your policy. - tiny and buildless. the whole runtime is roughly 13kb gzipped. drop in one script tag and it starts on its own. - familiar. fifteen directives, nine magic properties, and the @ and : shorthands. if you have used an html-first framework before, this will feel like home, with the rough edges filed off."},{"page":"Getting Started","category":"Getting Started","heading":"Where to go next","slug":"start","anchor":"where-to-go-next","snippet":"- Installation covers the script tag, npm, and bundlers. - Reactivity and State explains s-data, computed values, and methods. - The Directives and Magic Properties sections are a complete reference for every attribute a","text":"where to go next - installation covers the script tag, npm, and bundlers. - reactivity and state explains s-data, computed values, and methods. - the directives and magic properties sections are a complete reference for every attribute and helper."},{"page":"Templating","category":"Essentials","heading":"Templating","slug":"templating","anchor":"","snippet":"Templating is how state reaches the page: text, HTML, attributes, classes, and styles. Summit gives you three directives for it, each reactive.","text":"templating templating is how state reaches the page: text, html, attributes, classes, and styles. summit gives you three directives for it, each reactive."},{"page":"Templating","category":"Essentials","heading":"Text","slug":"templating","anchor":"text","snippet":"s-text sets an element's text content. It escapes automatically, so it is always safe for untrusted values. Any expression works, not just a variable:","text":"text s-text sets an element's text content. it escapes automatically, so it is always safe for untrusted values. any expression works, not just a variable:"},{"page":"Templating","category":"Essentials","heading":"HTML","slug":"templating","anchor":"html","snippet":"s-html sets innerHTML. Only ever use it with content you trust, because it is not escaped.","text":"html s-html sets innerhtml. only ever use it with content you trust, because it is not escaped."},{"page":"Templating","category":"Essentials","heading":"Attributes","slug":"templating","anchor":"attributes","snippet":"s-bind sets an attribute from an expression. The shorthand is a leading colon, which is what you will normally write. Boolean attributes are handled for you: when the expression is falsy the attribute is removed, when tr","text":"attributes s-bind sets an attribute from an expression. the shorthand is a leading colon, which is what you will normally write. boolean attributes are handled for you: when the expression is falsy the attribute is removed, when truthy it is added."},{"page":"Templating","category":"Essentials","heading":"Classes","slug":"templating","anchor":"classes","snippet":"Bind class with an object and each key is toggled by its value. Classes already on the element in plain class are kept. An array of names works too:","text":"classes bind class with an object and each key is toggled by its value. classes already on the element in plain class are kept. an array of names works too:"},{"page":"Templating","category":"Essentials","heading":"Styles","slug":"templating","anchor":"styles","snippet":"Bind style with an object of camelCase or kebab-case properties. For the full attribute reference, including how bound values merge with static ones, see s-bind.","text":"styles bind style with an object of camelcase or kebab-case properties. for the full attribute reference, including how bound values merge with static ones, see s-bind."}]